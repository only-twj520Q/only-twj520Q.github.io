<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>twj的个人小站</title>
  
  
  <link href="/only-twj520Q.github.io/atom.xml" rel="self"/>
  
  <link href="https://github.com/"/>
  <updated>2019-11-22T06:44:24.816Z</updated>
  <id>https://github.com/</id>
  
  <author>
    <name>twj</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>我理解的babel</title>
    <link href="https://github.com/2019/11/08/%E6%88%91%E7%90%86%E8%A7%A3%E7%9A%84babel/"/>
    <id>https://github.com/2019/11/08/%E6%88%91%E7%90%86%E8%A7%A3%E7%9A%84babel/</id>
    <published>2019-11-08T07:12:55.975Z</published>
    <updated>2019-11-22T06:44:24.816Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>什么是babel？你可以理解它就是一个语法转器，简单来说就是 ES6、ES7等等的新语法转化为ES5或能让低端浏览器正常运行的代码。比如我们经常使用的async、promise语法，在低端浏览器上可能就无法使用，会引起故障，但是只要我们合理使用babel，我们就可以放心大胆地使用新语法。</p><p>下面阐述的内容都是基于Babel 7的使用和总结，因babel 6 和babel 7在使用上存在较大差异，所以需要提前说明一下。</p><h2 id="核心原理"><a href="#核心原理" class="headerlink" title="核心原理"></a>核心原理</h2><p>这边简单介绍一下babel的工作原理，其实就是利用了抽象语法树，又称AST。所有的babel插件也是基于AST。</p><h4 id="Babel的处理步骤"><a href="#Babel的处理步骤" class="headerlink" title="Babel的处理步骤"></a>Babel的处理步骤</h4><ul><li><p>解析。将源代码变成AST。babel的解析器使用的是babylon。在解析过程中有两个阶段：<strong>词法分析</strong>和<strong>语法分析</strong>，词法分析阶段把字符串形式的代码转换为<strong>令牌</strong>（tokens）流，令牌类似于AST中节点；而语法分析阶段则会把一个令牌流转换成 AST的形式，同时这个阶段会把令牌中的信息转换成AST的表述结构。</p></li><li><p>转化。操作AST，去改变代码。babel转化器使用的是babel-traverse。babel使用提供的API对AST进行深度优先遍历，在此过程中对节点进行添加、更新及移除操作。</p></li><li><p>生成。将更改后的AST，再变回代码。babel生成器使用的是babel-generator。将经过转换的AST通过babel-generator再转换成js代码，过程就是深度优先遍历整个AST，然后构建可以表示转换后代码的字符串。</p></li></ul><p>我们编写的代码在编译阶段就被解析成AST，为什么要转化成AST，是因为方便计算机更好地理解代码，方便开发人员更好地操作代码。因为对于计算机或者编辑器而言，代码其实本质上就是字符串。</p><p>可以使用这个链接<a href="https://astexplorer.net/" target="_blank" rel="noopener">在线测试</a>观察代码生成的AST是什么样的。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">"type"</span>: <span class="string">"Program"</span>,</span><br><span class="line">    <span class="string">"start"</span>: <span class="number">0</span>,</span><br><span class="line">    <span class="string">"end"</span>: <span class="number">52</span>,</span><br><span class="line">    <span class="string">"body"</span>: [...]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>你会发现抽象语法树中不同层级有着相似的结构，这样的结构叫做节点，一个AST是由多个或单个这样的节点组成，节点内部还可以嵌套有多个这样的子节点。</p><h4 id="实际使用"><a href="#实际使用" class="headerlink" title="实际使用"></a>实际使用</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> babylon <span class="keyword">from</span> <span class="string">'babylon'</span>;</span><br><span class="line"><span class="keyword">import</span> traverse <span class="keyword">from</span> <span class="string">'babel-traverse'</span>;</span><br><span class="line"><span class="keyword">import</span> generate <span class="keyword">from</span> <span class="string">'babel-generator'</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> code = <span class="string">`function double(x) &#123;</span></span><br><span class="line"><span class="string">  return x;</span></span><br><span class="line"><span class="string">&#125;`</span>;</span><br><span class="line"><span class="keyword">const</span> ast = babylon.parse(code);</span><br><span class="line">traverse(ast, &#123;</span><br><span class="line">  enter(path) &#123;</span><br><span class="line">    <span class="keyword">if</span> (</span><br><span class="line">      path.node.type === <span class="string">"Identifier"</span> &amp;&amp;</span><br><span class="line">      path.node.name === <span class="string">"x"</span></span><br><span class="line">    ) &#123;</span><br><span class="line">      path.node.name = <span class="string">"xx"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">const</span> output = generate(ast, code);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'output'</span>, output)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 输出结果</span></span><br><span class="line">&#123;</span><br><span class="line">  code: <span class="string">"function double(xx) &#123;↵  return xx;↵&#125;"</span></span><br><span class="line">  map: <span class="literal">null</span></span><br><span class="line">  rawMappings: <span class="literal">null</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="基本用法"><a href="#基本用法" class="headerlink" title="基本用法"></a>基本用法</h2><p>一般而言，babel的使用存在两种方式。</p><ul><li>命令行</li><li>结合构建工具</li></ul><p>通常说来，后者更常见。如果结合webpack使用，我们需要在<code>webpack.config.js</code>中的配置项<code>rules</code>属性中配置<code>babel-loader</code>，然后在项目根目录中新增<code>.babelrc</code>文件，这个文件就是对babel的配置。</p><p><code>webpack.config.js</code>部分配置</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">module</span>: &#123;</span><br><span class="line">  rules: [</span><br><span class="line">    &#123;</span><br><span class="line">      test: <span class="regexp">/\.js$/</span>,</span><br><span class="line">      include: path.resolve(<span class="string">'src'</span>),</span><br><span class="line">      loader: <span class="string">'babel-loader'</span></span><br><span class="line">    &#125;,</span><br><span class="line">    ...</span><br><span class="line">  ]</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure><p><code>.babelrc</code>部分配置</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"presets"</span>: [</span><br><span class="line">    [ </span><br><span class="line">      <span class="string">"@babel/preset-env"</span>, &#123;</span><br><span class="line">        <span class="string">"targets"</span>: &#123;</span><br><span class="line">          <span class="string">"chrome"</span>: <span class="string">"52"</span>,</span><br><span class="line">          <span class="string">"ie"</span>: <span class="string">"8"</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">"useBuiltIns"</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="string">"corejs"</span>: <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    ]</span><br><span class="line">  ]</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure><p><code>babel-loader</code>使用前需要使用<code>npm</code>安装，并且它默认会读取根目录下的<code>.babelrc</code>的配置，或者直接在<code>babel-loader</code>的配置options也是可以的。注意<code>.babelrc</code>文件的内容格式是JSON。</p><h2 id="常见名词"><a href="#常见名词" class="headerlink" title="常见名词"></a>常见名词</h2><h4 id="babel-core"><a href="#babel-core" class="headerlink" title="@babel/core"></a>@babel/core</h4><p>babel的核心包，它主要的作用就是编译。</p><h4 id="babel-cli"><a href="#babel-cli" class="headerlink" title="@babel/cli"></a>@babel/cli</h4><p>只有@babel/core是无法在命令行使用这些功能的，@babel/cli支持你直接在命令行中编译代码。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">./node_modules/.bin/babel src --out-dir lib</span><br></pre></td></tr></table></figure><p>上面的命令会编译src目录下的所有js代码，并编译成最终的代码（<code>babel.config.js</code>或者<code>.babelrc</code>配置的），并输出到lib目录下。<code>--out-dir</code>代表输出到哪个目录下。</p><h4 id="babel-node"><a href="#babel-node" class="headerlink" title="@babel/node"></a>@babel/node</h4><p><code>babel-node</code> 命令并非独立安装，在babel 7以前，需要通过安装 <code>babel-cli</code> 包获得。而在babel 7以后，babel 的模块被拆分。因此需要安装 <code>@babel/core</code>和 <code>@babel/node</code> 两个包来获取。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 安装</span></span><br><span class="line">npm i -g @babel/core @babel/node</span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">babel-node test.js</span><br></pre></td></tr></table></figure><h4 id="babel-polyfill"><a href="#babel-polyfill" class="headerlink" title="@babel/polyfill"></a>@babel/polyfill</h4><p>babel默认只转化js语法，比如对let ，const，class等。它不转换新的API，比如Set、Map、Reflect、Symbol、Promise 等全局对象，包括全局对象上的方法，比如<code>Object.assign</code>。</p><p>我们使用babel官网提供的的<code>try out</code>功能做演示。</p><p><img src="https://img.ikstatic.cn/MTU3MzczMTMxOTQ5OCM1MzkjanBn.jpg" alt=""></p><p>如图，我们可以看到babel插件默认只会对class，箭头函数这些js语法糖进行转化。但是对新的API它默认不会进行支持，比如上图中的Promise。所以我们可以引入<code>@babel/polyfill</code>。这个库包含了<code>core-js</code>和<code>regenerator-runtime</code>。它是对完整的ES6环境的模拟，我们需要在入口文件或者所有代码的前面引入这个库。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="string">'@babel/polyfill'</span></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">import</span> <span class="string">"core-js/shim"</span>;</span><br><span class="line"><span class="keyword">import</span> <span class="string">"regenerator-runtime/runtime"</span>;</span><br></pre></td></tr></table></figure><p>但是这个库有两个很不可忽略的问题。</p><ul><li>体积太大。在非压缩的前提下，能达到几百K，而且如果我们只是使用了其中某个特性的情况下，这会造成很大的浪费。</li><li>会污染全局变量，因为它会增加很多全局变量API，或者在很多类的原型链上都作了修改，从而实现增加实例方法。如果我们开发的也是一个类库供其他开发者使用，这种情况就会变得非常不可控。</li></ul><p>至于说解决方案下面会提到。</p><h2 id="核心使用"><a href="#核心使用" class="headerlink" title="核心使用"></a>核心使用</h2><p>需要记住的是，babel本身不具有任何转化能力，我们把它转化的功能都分解到一个个插件中去。所以，当我们不配置任何插件的时候，输入和输出的代码都是一样的。</p><p>在配置文件中，我们主要是由两个配置项，分别是预设preset和插件plugins。</p><p>新的babel 7已经使用了新的插件语法。</p><h3 id="预设preset"><a href="#预设preset" class="headerlink" title="预设preset"></a>预设preset</h3><p>先解释一下什么是预设，在没有预设这个概念之前，当我们需要使用箭头函数的时候，我们使用<code>@babel/plugin-transform-arrow-functions</code>插件，它的作用就是将ES6的箭头函数转换成普通函数，但是ES6的语法太多了，如果我们单纯依靠引入一个个依赖的插件就太麻烦，而且容易出错。所以预设的出现就解决了这个问题，它可以理解为插件的组合，比如<code>@babel/preset-es2015</code>是对整个ES6语法的插件集合，还有<code>@babel/preset-es2016</code>等等，不过现在这些类似的这些预设官方也不推荐使用了，推荐使用<code>@babel/preset-env</code>。目前常见的预设有<code>@babel/preset-env</code>，<code>@babel/preset-react</code>，<code>@babel/preset-typescript</code>。</p><h4 id="babel-preset-env（重点）"><a href="#babel-preset-env（重点）" class="headerlink" title="@babel/preset-env（重点）"></a>@babel/preset-env（重点）</h4><p>重点介绍<code>@babel/preset-env</code>。这是一个能根据配置的运行环境特点，为代码做相应和必要的编译，同时支持浏览器和node，比如如果你要求的运行环境浏览器版本比较低，转化的代码可能会比较多，如果要求支持的是高版本浏览器，可能你都不需要转化ES6的语法，从而节省了很多空间。如果不写任何配置项，env 等价于 latest，也等价于<code>es2015 + es2016 + es2017</code> 三个相加(不包含 <code>stage-x</code> 中的插件)。env 包含的插件列表维护在<a href="https://github.com/babel/babel-preset-env/blob/master/data/plugin-features.js">这里</a></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 浏览器</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">"presets"</span>: [</span><br><span class="line">    [<span class="string">"@babel/preset-env"</span>, &#123;</span><br><span class="line">      <span class="string">"targets"</span>: &#123;</span><br><span class="line">        <span class="string">"browsers"</span>: [<span class="string">"last 2 versions"</span>, <span class="string">"safari &gt;= 7"</span>]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;]</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// node</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">"presets"</span>: [</span><br><span class="line">    [<span class="string">"@babel/preset-env"</span>, &#123;</span><br><span class="line">      <span class="string">"targets"</span>: &#123;</span><br><span class="line">        <span class="string">"node"</span>: <span class="string">"6.10"</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;]</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此外babel 7的还有一个重要的变化是删除了<code>stage-x</code>的相关插件，这是一个实验性的语法插件，所有针对处于标准提案阶段的功能所编写的预设（stage preset）都已被弃用。<code>@babel/preset-env</code>将只支持到<code>stage-4</code>。stage 是向下兼容 0&gt;1&gt;2&gt;3&gt;4 所包含的插件数量依次减少。</p><p>简单说明一下，TC39 将提案分为以下几个阶段：</p><ul><li>Stage 0 - 设想（Strawman）：只是一个想法，可能有 Babel插件。</li><li>Stage 1 - 建议（Proposal）：这是值得跟进的。</li><li>Stage 2 - 草案（Draft）：初始规范。</li><li>Stage 3 - 候选（Candidate）：完成规范并在浏览器上初步实现。</li><li>Stage 4 - 完成（Finished）：将添加到下一个年度版本发布中。</li></ul><h4 id="useBuiltIns配置"><a href="#useBuiltIns配置" class="headerlink" title="useBuiltIns配置"></a>useBuiltIns配置</h4><p>这是一个<code>preset-env</code>的重要配置选项，它的出现是为了解决上述提到<code>@babel/polyfill</code>的缺陷，它将会自动检测语法帮你require你代码中使用的部分polyfill。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="string">"presets"</span>: [</span><br><span class="line">    [<span class="string">"@babel/preset-env"</span>, &#123;</span><br><span class="line">      <span class="string">"useBuiltIns"</span>: <span class="string">"usage"</span>,</span><br><span class="line">      <span class="string">"corejs"</span>: <span class="number">2</span>,</span><br><span class="line">      <span class="string">"targets"</span>: &#123;</span><br><span class="line">        <span class="string">"browsers"</span>: [<span class="string">"last 2 versions"</span>, <span class="string">"safari &gt;= 7"</span>]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;]</span><br><span class="line">  ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个字段的可选值包括：usage、entry 和 false， 默认为 false，表示不对 polyfills 处理。</p><ul><li>usage：它会在你使用到 ES6 新特性时，自动从<code>core-js</code>中添加相关的模块和方法，不会造成全局污染。</li><li>entry：需要在入口使用<code>import &quot;@babel/polyfill&quot;</code>，将 polyfill 拆分引入，仅引入有浏览器不支持的 polyfill</li></ul><p><img src="https://img.ikstatic.cn/MTU3MzczMTQyMTY3MiM4NjEjanBn.jpg" alt="image-20191114175843871"></p><p>如上图，配置了entry以后，<code>import @babel/polyfill</code>会被分拆成很多<code>require(&quot;core-js/modules/xxx&quot;)</code>，它其实会根据<code>@babel/preset-env</code>配置的目标环境将一个大的库分拆引入。事实上，<code>@babel/polyfill</code>这个包本身是没有内容的，它依赖于<code>core-js</code>和<code>regenerator-runtime</code>这两个包，这两个包提供了ES6规范的运行时环境。因此当我们不需要按需polyfill时直接引入<code>@babel-polyfill</code>就行了，它会把<code>core-js</code>和<code>regenerator-runtime</code>全部导入，当我们配置了entry以后，它会根据目标环境自动按需引入<code>core-js</code>和<code>regenerator-runtime</code>。</p><p><img src="https://img.ikstatic.cn/MTU3MzczMTU1MzU4NCMgIDQjanBn.jpg" alt=""></p><p>如上图，当配置了usage以后，我们无需手动<code>import &#39;@babel/polyfill&#39;</code>，它会根据我们的实际代码结合配置的目标环境，自动引入相应的库。</p><p>所以，比较这两个配置字段，我更倾向于使用usage这种方式，事实上，它才是真正做到了按需引入。</p><h3 id="插件plugin"><a href="#插件plugin" class="headerlink" title="插件plugin"></a>插件plugin</h3><p>插件的作用是为了实现某个具体功能，比如预设不能支持的，比如<code>@babel/plugin-proposal-decorators</code>是为了解决装饰器的问题，比如<code>@babel/plugin-syntax-dynamic-import</code>是为了解决webpack动态引入某个模块的问题。</p><h4 id="babel-plugin-transform-runtime（重点）"><a href="#babel-plugin-transform-runtime（重点）" class="headerlink" title="@babel/plugin-transform-runtime（重点）"></a>@babel/plugin-transform-runtime（重点）</h4><p>这个插件能解决<code>@babel/polyfill</code>提供的类或者实例方法污染全局作用域的情况的。它的作用和上面的useBuiltIns很类似。</p><p><code>@babel/plugin-transform-runtime</code>插件是为了解决</p><ul><li>运行时引入，为了解决多个文件重复引用相同helpers </li><li>局部引入，为了解决新API方法全局污染</li></ul><p><img src="https://img.ikstatic.cn/MTU3MzczMTcyNjEwNyM2NTIjanBn.jpg" alt=""></p><p><img src="https://img.ikstatic.cn/MTU3MzczMTg4MTc5MyM2MjUjanBn.jpg" alt=""></p><p>对比上面的两个图，我们用class举例，可以发现，如果使用这个插件，它会在每个模块的内部重复定义这个相同的函数方法，虽然最后的结果也没有问题，但是会造成出现很多重复代码，并且增大了体积。而如果使用了插件以后，我们发现从定义方法改成引用，那重复定义就变成了重复引用，就不存在代码重复的问题了。如上图，可以发现，class这个helper 就是通过 <code>require</code> 引入的，这样就不会存在代码重复的问题了。</p><p>在使用 <code>babel-plugin-transform-runtime</code> 的时候必须把 <code>babel-runtime</code> 当做依赖。</p><p> <code>babel-runtime</code>内部集成了</p><ul><li><code>core-js</code>：转换一些内置类 (<code>Promise</code>, <code>Symbols</code>等等) 和静态方法 (<code>Array.from</code> 等)。绝大部分转换是这里做的，自动引入。</li><li><code>regenerator</code>：作为 <code>core-js</code> 的拾遗补漏，主要是对 <code>generator/yield</code> 和 <code>async/await</code> 两组的支持。当代码中有使用 <code>generators/async</code> 时自动引入。</li><li><code>helpers</code>： 如上面的 <code>asyncToGenerator</code> 就是其中之一，其他还有如 <code>jsx</code>, <code>classCallCheck</code> 等等，可以查看 <a href="https://github.com/babel/babel/blob/6.x/packages/babel-helpers/src/helpers.js">babel-helpers</a>。</li></ul><p>但是这个插件也有<strong>缺点：</strong><code>babel-plugin-transform-runtime</code> <strong>不支持</strong> 类的实例方法 (比如数组的<code>filter</code>方法等)</p><p>还有一个需要<strong>注意</strong>的是，我发现默认情况下这个插件不会对ES6新的API进行polyfill，就是说它默认不会转化新的特性比如Promise、<code>Object.assign</code>。所以我们还需要配置corejs这个字段，并安装对应的依赖<code>@babel/runtime-corejs2</code>。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">npm install --save @babel/runtime-corejs2</span><br><span class="line"></span><br><span class="line">&#123;</span><br><span class="line"> <span class="string">"plugins"</span>: [</span><br><span class="line">    [</span><br><span class="line">      <span class="string">"@babel/plugin-transform-runtime"</span>,</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="string">"helpers"</span>: <span class="literal">true</span>,</span><br><span class="line">        <span class="string">"regenerator"</span>: <span class="literal">true</span>,</span><br><span class="line">        <span class="string">"useESModules"</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="string">"corejs"</span>: <span class="number">2</span></span><br><span class="line">      &#125;</span><br><span class="line">    ]</span><br><span class="line">] </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="注意"><a href="#注意" class="headerlink" title="注意"></a>注意</h3><p>当plugins 与 presets 同时存在的情况，执行顺序如下</p><blockquote><p>这意味着如果两个转换插件都将处理程序的某个代码片段，则将根据转换插件或 preset 的排列顺序依次执行。</p><ul><li>先执行 plugins 的配置项,再执行 Preset 的配置项；</li><li>plugins 配置项，按照声明顺序执行。</li><li>Preset 配置项，按照声明逆序执行。</li></ul></blockquote><h3 id="Babel插件方案对比"><a href="#Babel插件方案对比" class="headerlink" title="Babel插件方案对比"></a>Babel插件方案对比</h3><table><thead><tr><th align="center">方案</th><th align="center">优点</th><th align="center">缺点</th></tr></thead><tbody><tr><td align="center">@babel/plugin-transform-runtime</td><td align="center">按需引入, 打包体积小</td><td align="center">不能兼容实例方法</td></tr><tr><td align="center">@babel/polyfill</td><td align="center">完整模拟 ES6 环境</td><td align="center">打包体积过大, 污染全局对象和内置的对象原型</td></tr><tr><td align="center">@babel/preset-env</td><td align="center">按需引入, 可配置性高，并且通过配置useBuiltIns实现按需加载@babel/polyfill</td><td align="center">暂未发现</td></tr></tbody></table><h3 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h3><p><code>@babel/plugin-transform-runtime</code>和<code>useBuiltIns</code>配置可以解决替代@babel/polyfill。</p><h2 id="babel使用总结"><a href="#babel使用总结" class="headerlink" title="babel使用总结"></a>babel使用总结</h2><p>综上，我个人对于babel 7的配置方案总结如下</p><ul><li>@babel/preset-env + targets + useBuiltins: usage</li><li>@babel/plugin-transform-runtime</li><li>引入必要的plugin</li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;什么是babel？你可以理解它就是一个语法转器，简单来说就是 ES6、ES7等等的新语法转化为ES5或能让低端浏览器正常运行的代码。比如我们
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="打包构建" scheme="https://github.com/tags/%E6%89%93%E5%8C%85%E6%9E%84%E5%BB%BA/"/>
    
  </entry>
  
  <entry>
    <title>异步错误的获取</title>
    <link href="https://github.com/2019/03/24/%E5%BC%82%E6%AD%A5%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86/"/>
    <id>https://github.com/2019/03/24/%E5%BC%82%E6%AD%A5%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86/</id>
    <published>2019-03-24T05:47:16.101Z</published>
    <updated>2019-11-22T06:44:48.615Z</updated>
    
    <content type="html"><![CDATA[<h2 id="异步错误的获取"><a href="#异步错误的获取" class="headerlink" title="异步错误的获取"></a>异步错误的获取</h2><h2 id="JS常见错误"><a href="#JS常见错误" class="headerlink" title="JS常见错误"></a>JS常见错误</h2><p>当 JavaScript 引擎执行 JavaScript 代码时，会发生各种错误，常见的错误类型有</p><ul><li>SyntaxError。<br>SyntaxError是解析代码时发生的语法错误<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">var 1a;   //变量名错误 </span><br><span class="line">console.log &apos;hello&apos;);   // 缺少括号</span><br></pre></td></tr></table></figure></li><li>ReferenceError<br>ReferenceError是引用一个不存在的变量时发生的错误。<br>比如在函数中调用一个变量，但是这个变量不存在的时候</li><li>TypeError<br>TypeError是变量或参数不是预期类型时发生的错误。比如，对字符串、布尔值、数值等原始类型的值使用new命令，就会抛出这种错误，因为new命令的参数应该是一个构造函数。<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">var obj = &#123;&#125;; obj.handle()  //obj.handle is not a function</span><br></pre></td></tr></table></figure>还有一些这里就不一一介绍了。</li></ul><h2 id="try-catch"><a href="#try-catch" class="headerlink" title="try catch"></a>try catch</h2><h3 id="使用"><a href="#使用" class="headerlink" title="使用"></a>使用</h3><p>总之，我们在写代码的时候，程序可能遇到无法预测的异常情况而报错，从而阻塞代码执行，例如，网络连接中断，读取不存在的文件等。<br>上面的错误如果从产生的阶段上来划分的话，可以分成编译阶段和执行阶段的错误。<br>对于执行阶段的错误，我们通常使用try catch来捕获这种错误并处理它。<br>当代码块被try { … }包裹的时候，一旦发生错误，就不再继续执行后续代码，转而跳到catch块。catch (e) { … }包裹的代码就是错误处理代码，变量e表示捕获到的错误。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">try&#123;</span><br><span class="line">  console.log(&apos;step1&apos;);</span><br><span class="line">  var s = null;</span><br><span class="line">  console.log(s.length)  //产生错误</span><br><span class="line">  console.log(&apos;step2&apos;);</span><br><span class="line">&#125; catch(e)&#123;</span><br><span class="line">  console.log(&apos;error&apos;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//output</span><br><span class="line">step1</span><br><span class="line">error</span><br></pre></td></tr></table></figure><h3 id="抛出错误"><a href="#抛出错误" class="headerlink" title="抛出错误"></a>抛出错误</h3><p>程序也可以主动抛出一个错误，让执行流程直接跳转到catch块。抛出错误使用throw语句。<br>实际上，JavaScript允许抛出任意对象，包括数字、字符串。但是，最好还是抛出一个Error对象。</p><h3 id="catch捕获"><a href="#catch捕获" class="headerlink" title="catch捕获"></a>catch捕获</h3><p>JavaScript有一个标准的Error对象表示错误，还有从Error派生的TypeError、ReferenceError等错误对象。<br>我们在处理错误时，可以通过catch(e)捕获的变量e访问错误对象。我们抛出的错误其实是继承在这个Error对象。我们在throw的new Error传的参数就是error实例的message属性。</p><h3 id="错误传播"><a href="#错误传播" class="headerlink" title="错误传播"></a>错误传播</h3><p>如果在一个函数内部发生了错误，它自身没有捕获，错误就会被抛到外层调用函数，如果外层函数也没有捕获，该错误会一直沿着函数调用链向上抛出，直到被JavaScript引擎捕获，代码终止执行。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">try &#123;</span><br><span class="line">  try&#123;</span><br><span class="line">    throw new Error(&apos;这是抛出的错误&apos;);</span><br><span class="line">  &#125; catch(e) &#123;</span><br><span class="line">    console.log(&apos;内层捕获的错误&apos;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125; catch(e) &#123;</span><br><span class="line">  console.log(&apos;外层捕获的错误&apos;);</span><br><span class="line">&#125;</span><br><span class="line">//output</span><br><span class="line">内层捕获的错误</span><br></pre></td></tr></table></figure><h3 id="finally"><a href="#finally" class="headerlink" title="finally"></a>finally</h3><p>最后，无论有没有错误，finally一定会被执行。</p><h2 id="异步错误"><a href="#异步错误" class="headerlink" title="异步错误"></a>异步错误</h2><p>如果try模块里面是通过异步操作抛出的异常，异常就不能正常捕获到。比如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">try&#123;</span><br><span class="line">    setTimeout(()=&gt;&#123;</span><br><span class="line">        throw new Error(&apos;fail&apos;);</span><br><span class="line">    &#125;,1000);</span><br><span class="line">&#125; catch (e)&#123;</span><br><span class="line">    console.log(e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这是因为异步调用是立即返回的，因此当发生异常的时候，已经脱离了try..catch..的上下文了，所以异常无法被捕获。</p><h2 id="异步错误的解决方案"><a href="#异步错误的解决方案" class="headerlink" title="异步错误的解决方案"></a>异步错误的解决方案</h2><h3 id="异步代码内部直接捕获错误"><a href="#异步代码内部直接捕获错误" class="headerlink" title="异步代码内部直接捕获错误"></a>异步代码内部直接捕获错误</h3><p>如果是异步的异常，那就在异步代码或者回调函数里捕获异常。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">setTimeout(()=&gt;&#123;</span><br><span class="line">    try&#123;</span><br><span class="line">        throw new Error(&apos;fail&apos;);</span><br><span class="line">    &#125;catch (e)&#123;</span><br><span class="line">        console.log(e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;,1000);</span><br></pre></td></tr></table></figure><h3 id="使用Promise"><a href="#使用Promise" class="headerlink" title="使用Promise"></a>使用Promise</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line">var p1 = function()&#123;</span><br><span class="line">  return new Promise(function(resolve,reject)&#123;</span><br><span class="line">    throw new Error(&apos;p1_同步_err&apos;);       //代码1</span><br><span class="line">  setTimeout(()=&gt;&#123;</span><br><span class="line">      console.log(&apos;p1执行&apos;)      </span><br><span class="line">      resolve(true)</span><br><span class="line">      // throw new Error(&apos;p1_异步_err&apos;);  //代码2</span><br><span class="line">      // reject(&apos;p1_rej&apos;)                 //代码3</span><br><span class="line">    &#125;,1000)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line">var p2 = function()&#123;</span><br><span class="line">  return new Promise(function(resolve,reject)&#123;</span><br><span class="line">    // throw new Error(&apos;p2_同步_err&apos;);       //代码4</span><br><span class="line">    setTimeout(()=&gt;&#123;</span><br><span class="line">      console.log(&apos;p2执行&apos;)</span><br><span class="line">      // throw new Error(&apos;p2_异步_err&apos;);   //代码5</span><br><span class="line">      // reject(&apos;p2_rej&apos;)                    //代码6</span><br><span class="line">    &#125;,1000)</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line">p1().then(p2).catch(function(err)&#123;</span><br><span class="line">  console.log(&apos;catch里的错误&apos;)</span><br><span class="line">  console.log(err)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">//output1</span><br><span class="line">catch里的错误</span><br><span class="line">a.html:44 Error: p1_同步_err</span><br><span class="line">    at a.html:23</span><br><span class="line">    at new Promise (&lt;anonymous&gt;)</span><br><span class="line">    at p1 (a.html:22)</span><br><span class="line">    at a.html:42</span><br><span class="line"></span><br><span class="line">//output2</span><br><span class="line">p1执行</span><br><span class="line">a.html:27 Uncaught Error: p1_异步_err</span><br><span class="line">    at setTimeout (a.html:27)</span><br><span class="line"></span><br><span class="line">//output3</span><br><span class="line">p1执行</span><br><span class="line">a.html:43 catch里的错误</span><br><span class="line">a.html:44 p1_rej</span><br></pre></td></tr></table></figure><p>做了一组试验。六种情况。比如<br>情况1，代码1执行，其它的代码2-6都注释掉。<br>情况2，代码2执行，其它的代码1,3-6都注释掉。<br>情况3，代码3执行，其它的代码1，2，4-6都注释掉。</p><p>结论：对于promise而言</p><ul><li>如果是同步执行，throw出去的错误可以捕获，而异步执行的不可以</li><li>如果是异步执行，throw出去的错误不可以被捕获，只能通过reject来传递。</li></ul><h3 id="使用async和await"><a href="#使用async和await" class="headerlink" title="使用async和await"></a>使用async和await</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">async function doSomething() &#123;</span><br><span class="line">    return new Promise((resolve, reject) =&gt; &#123;</span><br><span class="line">        setTimeout(() =&gt; &#123;</span><br><span class="line">            throw new Error(&quot;fail&quot;);</span><br><span class="line">        &#125;, 1000);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">async function main() &#123;</span><br><span class="line">    try &#123;</span><br><span class="line">        await doSomething();</span><br><span class="line">    &#125; catch (e) &#123;</span><br><span class="line">        console.log(e.message);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">main();</span><br></pre></td></tr></table></figure><p>因为async await实现方式本身也是基于generator+promise</p><h3 id="浏览器环境"><a href="#浏览器环境" class="headerlink" title="浏览器环境"></a>浏览器环境</h3><p>在浏览器中，很多时候出现Error，整个页面就挂掉了。在window对象下有个 onerror 属性。我们可以给window.onerror属性加上一个回调来实现对前端代码的监控。onerror函数会在页面发生js错误时被调用。<br>window.onerror可以拿到出错的信息以及文件名、行号、列号，如果在onerror函数中return true可以让浏览器不输出错误信息到控制台。<br>否则会在控制台中显示错误消息。</p><h3 id="nodeJs环境"><a href="#nodeJs环境" class="headerlink" title="nodeJs环境"></a>nodeJs环境</h3><p>需要说明的是，在node中大多数的异步方法都接受一个 callback 函数，该函数会接受一个 Error 对象传入作为第一个参数。 如果第一个参数不是 null 而是一个 Error 实例，则说明发生了错误，应该进行处理。</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">const fs = require(&apos;fs&apos;);</span><br><span class="line">  fs.readFile(&apos;一个不存在的文件&apos;, (err, data) =&gt; &#123;</span><br><span class="line">    if (err) &#123;</span><br><span class="line">      console.error(&apos;读取文件出错！&apos;, err);</span><br><span class="line">      return;</span><br><span class="line">    &#125;</span><br><span class="line">    // 否则处理数据</span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure><ul><li>uncaughtException<br>uncaughtException 其实是 NodeJS 进程的一个事件。如果进程里产生了一个异常而没有被任何Try Catch捕获会触发这个事件。<br>NodeJS 对于未捕获异常的默认处理是：沿着代码调用路径反向传递回事件循环 - 触发 uncaughtException 事件 - 如果 uncaughtException 没有被监听，那么 - 打印异常的堆栈信息 - 触发进程的 exit 事件。Node.js原生提供uncaughtException事件挂到process对象上，用于捕获所有未处理的异常。</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">process.on(&apos;uncaughtException&apos;, function (err) &#123;</span><br><span class="line">    console.log(&apos;uncaughtException error&apos;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">try &#123;</span><br><span class="line">  setTimeout(function()&#123;</span><br><span class="line">    throw new Error(&apos;fail&apos;);</span><br><span class="line">  &#125;,1000);</span><br><span class="line">&#125; catch (e) &#123;</span><br><span class="line">    console.log(&quot;catch error&quot;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">//output</span><br><span class="line">uncaughtException error</span><br></pre></td></tr></table></figure><p>uncaughtException<br>需要注意，如果打算使用 ‘uncaughtException’ 事件作为异常处理的最后补救机制，这是非常粗糙的设计方式。未处理异常本身就意味着应用已经处于了未定义的状态。如果基于这种状态，尝试恢复应用正常进行，可能会造成未知或不可预测的问题。<br>而且uncaughtException错误会导致当前的所有的用户连接都被中断，甚至不能返回一个正常的 HTTP 错误码，这是由于uncaughtException 丢失了当前环境的堆栈，导致 Node 不能正常进行内存回收，比如下面的例子</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">app.get(&apos;/&apos;, function (req, res) &#123;</span><br><span class="line">    setTimeout(function () &#123;</span><br><span class="line">        throw new Error(&apos;async error&apos;); </span><br><span class="line">        res.send(200);</span><br><span class="line">    &#125;, 1000);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">process.on(&apos;uncaughtException&apos;, function (err) &#123;</span><br><span class="line">    res.send(500); // 做不到，拿不到当前请求的 res 对象</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><ul><li>domain<br>如果可以通过某种方式来捕获回调函数中的异常，那么就不会有uncaughtException 错误导致的崩溃。为了解决这个问题，Node 0.8 之后的版本新增了domain 模块，它可以用来捕获回调函数中抛出的异常。<br>domain模块，把处理多个不同的IO的操作作为一个组。注册事件和回调到domain，当发生一个错误事件或抛出一个错误时，domain对象会被通知，不会丢失上下文环境，也不导致程序错误立即退出。<br>domain 主要的 API 有 domain.run 和 error 事件。通过 domain.run 执行的函数中引发的异常都可以通过 domain 的 error 事件捕获，例如:</li></ul><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">var app = express();</span><br><span class="line">var server = require(&apos;http&apos;).createServer(app);</span><br><span class="line">var domain = require(&apos;domain&apos;);</span><br><span class="line"></span><br><span class="line">app.use(function (req, res, next) &#123;</span><br><span class="line">    var reqDomain = domain.create();</span><br><span class="line">    reqDomain.on(&apos;error&apos;, function (err) &#123; // 下面抛出的异常在这里被捕获</span><br><span class="line">        res.send(500, err.stack); // 成功给用户返回了 500</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    reqDomain.run(next);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">app.get(&apos;/&apos;, function () &#123;</span><br><span class="line">    setTimeout(function () &#123;</span><br><span class="line">        throw new Error(&apos;async exception&apos;); // 抛出一个异步异常</span><br><span class="line">    &#125;, 1000);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>上面的代码将 domain 作为一个中间件来使用，保证之后 express 所有的中间件都在domain.run 函数内部执行。这些中间件内的异常都可以通过 error 事件来捕获。我们可以正常的给用户返回 500 错误。</p><p>所以，我们可以结合两种异常捕获机制，用 domain 来捕获大部分的异常。对于剩下的异常，通过 uncaughtException 事件来避免服务器直接 crash。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;异步错误的获取&quot;&gt;&lt;a href=&quot;#异步错误的获取&quot; class=&quot;headerlink&quot; title=&quot;异步错误的获取&quot;&gt;&lt;/a&gt;异步错误的获取&lt;/h2&gt;&lt;h2 id=&quot;JS常见错误&quot;&gt;&lt;a href=&quot;#JS常见错误&quot; class=&quot;headerlink&quot; 
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="https://github.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>Promise杂谈</title>
    <link href="https://github.com/2019/03/09/Promise%E6%9D%82%E8%B0%88/"/>
    <id>https://github.com/2019/03/09/Promise%E6%9D%82%E8%B0%88/</id>
    <published>2019-03-09T05:34:08.260Z</published>
    <updated>2019-11-22T06:45:55.642Z</updated>
    
    <content type="html"><![CDATA[<h2 id="promise介绍"><a href="#promise介绍" class="headerlink" title="promise介绍"></a>promise介绍</h2><p><strong>Promise</strong>是异步编程的一种解决方案。ES6提供原生支持，并为<strong>Promise</strong>提供了统一的 API。</p><p>从语法上说，<strong>Promise</strong>是一个JS的原生对象，从它可以获取异步操作的消息。</p><p><strong>Promise</strong> 本身是一个构造函数，可以像下面这样构造一个<strong>Promise</strong>实例</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="string">'success'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(p);</span><br></pre></td></tr></table></figure><p>打印结果如下</p><p><img src="https://ws1.sinaimg.cn/large/006tKfTcly1g0whvqb1jdj3082024jrg.jpg" alt="promise结果"></p><p><strong>Promise</strong>实例有两个私有属性，其中<code>[[PromiseStates]]</code> 代表状态，一共有三种状态，[[PromiseValue]]代表结果。</p><ul><li><code>[[PromiseStates]]</code> <ul><li>pending：等待中</li><li>resolved：成功</li><li>rejected：失败</li></ul></li><li><code>[[PromiseValue]]</code></li></ul><p>基本介绍就写这么多了，网上资料太多了。</p><p>下面主要是对一些特性的试验，基本以代码为主。</p><h2 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h2><p>下面是我自己使用下来总结的一些特点，不具有代表性。</p><h3 id="特点一：立即执行性和异步性"><a href="#特点一：立即执行性和异步性" class="headerlink" title="特点一：立即执行性和异步性"></a>特点一：立即执行性和异步性</h3><p>所谓立即执行性，是指<strong>Promise</strong> 构造函数新建实例后立即执行，它也是同步代码。</p><p>所谓异步性，是指<strong>promise</strong> 的<code>then()</code>具有异步性，当执行到<code>.then()</code>部分，这部分会自动进入到<strong>Promise</strong>的异步事件队列，不会阻塞同步代码的执行。需要注意的是<strong>promise</strong>这种异步任务是属于一个微任务，它和以setTimeout为代表的宏任务在执行顺序上有一点区别。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'promise内部'</span>)</span><br><span class="line">  resolve(<span class="string">'success'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">p.then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'then内部'</span>, value);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'主程序'</span>);</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">promise内部</span><br><span class="line">主程序</span><br><span class="line">then内部 success</span><br></pre></td></tr></table></figure><h3 id="特点二：状态不可逆"><a href="#特点二：状态不可逆" class="headerlink" title="特点二：状态不可逆"></a>特点二：状态不可逆</h3><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="string">'p1 success-1'</span>);</span><br><span class="line">  resolve(<span class="string">'p1 success-2'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="string">'p2 success'</span>);</span><br><span class="line">  reject(<span class="string">'p2 reject'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p3 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  reject(<span class="string">'p3 reject'</span>);</span><br><span class="line">  resolve(<span class="string">'p3 success'</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">p1.then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">p2.then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">&#125;, (err) =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">p3.then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">&#125;, (err) =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(err);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">p1 success<span class="number">-1</span></span><br><span class="line">p2 success</span><br><span class="line">p3 reject</span><br></pre></td></tr></table></figure><p><strong>Promise</strong>一旦使用了resolve或者reject的时候，状态就不能再次变化，这就是<strong>Promise</strong>的不可逆性。</p><h3 id="特点三：链式调用"><a href="#特点三：链式调用" class="headerlink" title="特点三：链式调用"></a>特点三：链式调用</h3><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">5</span>);</span><br><span class="line">&#125;));</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p1 = p.then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">  <span class="keyword">return</span> value * <span class="number">10</span>;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p1)</span><br></pre></td></tr></table></figure><p>输出结果</p><p>![image-20190313151958689](/Users/apple/Library/Application Support/typora-user-images/image-20190313151958689.png)</p><p>可以看到<code>then</code>返回的就是一个<strong>Promise</strong>，而且是立即返回。这边之所以展开是resolved状态，是因为console.log的异步性，或者说在展开对象属性之前，它保留的是一个对象的引用。如果需要实时查看状态，加个断点即可。</p><p>也正是因为<code>then</code>返回的是一个新的<strong>Promise</strong>，所以可以通过链式调用<code>then</code> 方法。</p><p><code>then</code>方法接收两个函数作为参数，第一个参数是<strong>Promise</strong>执行成功时的回调，第二个参数是<strong>Promise</strong>执行失败时的回调。两个函数只会有一个被调用，函数的返回值将被用作创建then返回的Promise对象。</p><h4 id="then中的返回值"><a href="#then中的返回值" class="headerlink" title="then中的返回值"></a>then中的返回值</h4><p>大概总结了下面四种情况</p><ul><li><p><code>return</code>了一个同步的值，经过测试，不管返回null，undefined或者其他类型，<code>then</code>方法将返回一个resolved状态的<strong>Promise</strong>对象，<strong>Promise</strong>对象的值就是这个返回值。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    resolve(<span class="number">1</span>);</span><br><span class="line">  &#125;));</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">p</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">null</span>;</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(value);</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'对象类型'</span>, &#123;&#125;.toString.call(value));</span><br><span class="line">  &#125;)</span><br></pre></td></tr></table></figure><p>输出结果</p></li><li><pre><code class="javascript"><span class="literal">null</span>对象类型 [object Null]&lt;!--￼<span class="number">7</span>--&gt;</code></pre></li></ul><ul><li><p><code>return</code>了另一个 <strong>Promise</strong>，<code>then</code>方法将根据这个<strong>Promise</strong>的状态和值创建一个新的<strong>Promise</strong>对象返回。</p><p>比如返回的是<code>Promise.resolved(value)</code>，则<strong>Promise</strong>的状态为resolved，值为value。</p></li><li><pre><code class="javascript"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>{  resolve(<span class="number">1</span>);})<span class="keyword">const</span> p2 = p1.then(<span class="function">(<span class="params">value</span>) =&gt;</span> {  <span class="keyword">return</span> p1;})p2.then(<span class="function">(<span class="params">value</span>) =&gt;</span> {  <span class="built_in">console</span>.log(value)})<span class="built_in">console</span>.log(p1 === p2)&lt;!--￼<span class="number">8</span>--&gt;</code></pre></li><li><p><code>throw</code> 一个同步异常，<code>then</code>方法将返回一个<strong>Promise</strong>，状态是rejected，值是<code>throw</code>的参数。</p></li></ul><h2 id="不常见的例子"><a href="#不常见的例子" class="headerlink" title="不常见的例子"></a>不常见的例子</h2><h4 id="例子1"><a href="#例子1" class="headerlink" title="例子1"></a>例子1</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'p1-promise'</span>);</span><br><span class="line">    reject(<span class="string">'这个是错误'</span>)</span><br><span class="line">  &#125;, <span class="number">3000</span>)</span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'p2-promise'</span>);</span><br><span class="line">  resolve(p1);</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">p2</span><br><span class="line">  .then(<span class="function"><span class="params">result</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'then内部'</span>, result))</span><br><span class="line">  .catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><p><img src="https://ws4.sinaimg.cn/large/006tKfTcly1g11bev0otug30c604eaaa.gif" alt=""></p><h4 id="例子2"><a href="#例子2" class="headerlink" title="例子2"></a>例子2</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'p1-promise'</span>);</span><br><span class="line">    resolve(<span class="string">'这个是错误'</span>)</span><br><span class="line">  &#125;, <span class="number">3000</span>)</span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'p2-promise'</span>);</span><br><span class="line">  reject(p1);</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">p2</span><br><span class="line">  .then(<span class="function"><span class="params">result</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'then内部'</span>, result))</span><br><span class="line">  .catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><p><img src="https://ws4.sinaimg.cn/large/006tKfTcly1g11bjjs1ewg30c604et9p.gif" alt=""></p><h4 id="例子3"><a href="#例子3" class="headerlink" title="例子3"></a>例子3</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'p1-promise'</span>);</span><br><span class="line">    reject(<span class="string">'这个是错误'</span>)</span><br><span class="line">  &#125;, <span class="number">3000</span>)</span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span> (<span class="params">resolve, reject</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'p2-promise'</span>);</span><br><span class="line">  reject(p1);</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">p2</span><br><span class="line">  .then(<span class="function"><span class="params">result</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'then内部'</span>, result))</span><br><span class="line">  .catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><p><img src="https://ws4.sinaimg.cn/large/006tKfTcly1g11c12ksu1g30c604emxg.gif" alt=""></p><p>三个例子的结果各异，对于这样的现象，我个人的理解是这样。</p><p>p1和p2都是一个<strong>Promise</strong>。一方面，在p2代码中，如果使用reject方法，p2的状态变成了一个rejected，值就是p1，错误直接被catch住，而且error本身就是p1。三秒以后，如果p1的状态变成了resolved，可以添加<code>then</code>添加回调获取p1的值，p1的状态变成了rejected，则就抛出一个错误了。</p><p>另一方面，在p2代码中，如果使用resolve方法，p2的状态不会马上变成resolved，但是这个时候，它会依赖p1的结果，所以它会等到三秒以后p1的状态，如果p1变成了resolved，则会执行<code>then</code>中的代码，如果p1变成了rejected，则会执行<code>catch</code>中的代码，并且很重要的一点是它会对p1进行类似拆箱的操作，会直接拿到p1的结果，作为<code>then</code>或者<code>catch</code>回调的参数。</p><h4 id="例子4"><a href="#例子4" class="headerlink" title="例子4"></a>例子4</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p1 = <span class="built_in">Promise</span>.resolve(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">const</span> p2 = <span class="built_in">Promise</span>.resolve(p1);</span><br><span class="line"><span class="keyword">const</span> p3 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(<span class="number">1</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> p4 = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  resolve(p1);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(p1 === p2);</span><br><span class="line"><span class="built_in">console</span>.log(p1 === p3);</span><br><span class="line"><span class="built_in">console</span>.log(p3 === p4);</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="literal">true</span></span><br><span class="line"><span class="literal">false</span></span><br><span class="line"><span class="literal">false</span></span><br></pre></td></tr></table></figure><p>p1接收了一个普通值1，所以会返回一个resolved状态的<strong>Promise</strong>对象，值为1。<br>p2接收了一个<strong>Promise</strong>对象p1，会直接返回这个<strong>Promise</strong>对象。<br>p3和p4通过<code>new</code>方式创建了一个新的<strong>Promise</strong>对象，所以p3和p1,p4都不会相等。</p><h2 id="错误"><a href="#错误" class="headerlink" title="错误"></a>错误</h2><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  reject(<span class="string">'promise内部的错误'</span>);</span><br><span class="line">&#125;));</span><br><span class="line">p</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(value);</span><br><span class="line">    <span class="keyword">return</span> value * <span class="number">10</span>;</span><br><span class="line">  &#125;)</span><br><span class="line">setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> &#123;<span class="built_in">console</span>.log(<span class="string">'主程序'</span>)&#125;, <span class="number">1000</span>)</span><br></pre></td></tr></table></figure><p><img src="https://ws1.sinaimg.cn/large/006tKfTcly1g11877rvvtj30ns01d3yl.jpg" alt=""></p><p>如果<strong>Promise</strong>抛出一个错误，但是在<code>then</code>中没有第二个参数来捕获这个错误的话，就会在控制台打印错误信息，但是不会阻塞代码继续执行。</p><h4 id="then中对于错误的处理"><a href="#then中对于错误的处理" class="headerlink" title="then中对于错误的处理"></a>then中对于错误的处理</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">   resolve(<span class="number">1</span>);</span><br><span class="line"> &#125;));</span><br><span class="line">p</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第一个then的第一个回调'</span>, value);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Promise</span>.reject(<span class="string">'then中错误啦'</span>)</span><br><span class="line">  &#125;, (value) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第一个then的第二个回调'</span>, value);</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第二个then的第一个回调'</span>, value);</span><br><span class="line">  &#125;, (value) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第二个then的第二个回调'</span>, value);</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第三个then的第一个回调'</span>, value);</span><br><span class="line">  &#125;, (value) =&gt; &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第三个then的第二个回调'</span>, value);</span><br><span class="line">  &#125;).catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">第一个then的第一个回调 <span class="number">1</span></span><br><span class="line">第二个then的第二个回调 then中错误啦</span><br><span class="line">第三个then的第一个回调 <span class="literal">undefined</span></span><br></pre></td></tr></table></figure><p>因为在第二个<code>then</code>的参数中有对错误的处理，所以它可以捕获之前的error信息，并且自身的状态也变成了resolved。所以它能执行第三个<code>then</code>的回调。</p><p>如果不在<code>then</code>中进行对之前<strong>Promise</strong>的捕获，则一旦发生错误，会中断<strong>Promise</strong>链后面的代码，直接被<code>catch</code>到错误。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// throw new Error('promise内部的错误')</span></span><br><span class="line">  resolve(<span class="number">1</span>);</span><br><span class="line">&#125;));</span><br><span class="line">p</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第一个then的第一个回调'</span>, value);</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Promise</span>.reject(<span class="string">'then中错误啦'</span>)</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第二个then的第一个回调'</span>, value);</span><br><span class="line">  &#125;)</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'第三个then的第一个回调'</span>, value);</span><br><span class="line">  &#125;).catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">第一个then的第一个回调 <span class="number">1</span></span><br><span class="line"><span class="keyword">catch</span>内部 then中错误啦</span><br></pre></td></tr></table></figure><h4 id="throw了一个error"><a href="#throw了一个error" class="headerlink" title="throw了一个error"></a>throw了一个error</h4><p>如果在<strong>Promise</strong>中直接throw了一个错误的话，则会让<strong>Promise</strong>的状态变成rejected，不会直接<strong>Promise</strong>下面的代码。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> p = <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">(resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">'promise内部的错误'</span>)</span><br><span class="line">  resolve(<span class="number">1</span>);</span><br><span class="line">&#125;));</span><br><span class="line">p</span><br><span class="line">  .then(<span class="function">(<span class="params">value</span>) =&gt;</span> &#123;</span><br><span class="line">    &#125;)</span><br><span class="line">  .catch(<span class="function"><span class="params">error</span> =&gt;</span> <span class="built_in">console</span>.log(<span class="string">'catch内部'</span>, error))</span><br></pre></td></tr></table></figure><p>输出结果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">catch</span>内部 <span class="built_in">Error</span>: promise内部的错误</span><br><span class="line">    at <span class="built_in">Promise</span> ((index):<span class="number">38</span>)</span><br><span class="line">    at <span class="keyword">new</span> <span class="built_in">Promise</span> (<span class="xml"><span class="tag">&lt;<span class="name">anonymous</span>&gt;</span>)</span></span><br><span class="line"><span class="xml">    at (index):37</span></span><br></pre></td></tr></table></figure><h4 id="try-catch"><a href="#try-catch" class="headerlink" title="try catch"></a>try catch</h4><p>在<strong>Promise</strong>代码外部使用<code>try catch</code>并不会得到想要的结果。因为<code>try catch</code>捕获的是同步代码中的错误。</p><p>关于更多捕获在异步代码中错误的问题，可以参考我的另一篇日记。</p><h2 id="顺序执行"><a href="#顺序执行" class="headerlink" title="顺序执行"></a>顺序执行</h2><p>如何让Promise顺序执行，可以利用async和await。下面是一个很简单的demo。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> p1 = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> resolve(<span class="number">1</span>), <span class="number">1000</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">let</span> p2 = <span class="function"><span class="params">()</span> =&gt;</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">  setTimeout(<span class="function"><span class="params">()</span> =&gt;</span> resolve(<span class="number">2</span>), <span class="number">2000</span>)</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">let</span> p = [p1, p2];</span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">queue</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>; i&lt;p.length; i++)&#123;</span><br><span class="line">    <span class="keyword">let</span> result = <span class="keyword">await</span> p[i]();</span><br><span class="line">    <span class="built_in">console</span>.log(result)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">queue()</span><br></pre></td></tr></table></figure><h2 id="实际使用"><a href="#实际使用" class="headerlink" title="实际使用"></a>实际使用</h2><p>简单封装一下ajax</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> fetchData = <span class="function"><span class="keyword">function</span>(<span class="params">url, method, headerConfig</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="keyword">function</span>(<span class="params">resolve, reject</span>)</span>&#123;</span><br><span class="line">    <span class="keyword">const</span> handleChange = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.readyState !== <span class="number">4</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.status === <span class="number">200</span> || <span class="keyword">this</span>.status === <span class="number">304</span>) &#123;</span><br><span class="line">        resolve(<span class="keyword">this</span>.response);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        reject(<span class="keyword">new</span> <span class="built_in">Error</span>(<span class="keyword">this</span>.statusText));</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">const</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">    xhr.open(method, url);</span><br><span class="line">    xhr.onreadystatechange = handleChange;</span><br><span class="line">    xhr.responseType = <span class="string">"json"</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> headerConfig === <span class="string">'object'</span> &amp;&amp; headerConfig !== <span class="literal">null</span>) &#123;</span><br><span class="line">     <span class="keyword">for</span> (<span class="keyword">let</span> headerKey <span class="keyword">in</span> headerConfig) &#123;</span><br><span class="line">    xhr.setRequestHeader(headerKey, headerConfig[headerKey]);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    xhr.send();</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;promise介绍&quot;&gt;&lt;a href=&quot;#promise介绍&quot; class=&quot;headerlink&quot; title=&quot;promise介绍&quot;&gt;&lt;/a&gt;promise介绍&lt;/h2&gt;&lt;p&gt;&lt;strong&gt;Promise&lt;/strong&gt;是异步编程的一种解决方案。ES6提供
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="https://github.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>canvas和img日常转换操作</title>
    <link href="https://github.com/2019/02/12/canvas%E5%92%8Cimg%E6%97%A5%E5%B8%B8%E8%BD%AC%E6%8D%A2%E6%93%8D%E4%BD%9C/"/>
    <id>https://github.com/2019/02/12/canvas%E5%92%8Cimg%E6%97%A5%E5%B8%B8%E8%BD%AC%E6%8D%A2%E6%93%8D%E4%BD%9C/</id>
    <published>2019-02-12T07:50:28.691Z</published>
    <updated>2019-11-22T06:45:20.551Z</updated>
    
    <content type="html"><![CDATA[<p>近在做图片上传，接触了一些图片相关的转化操作。主要涉及blob，canvas，dataurl。做一下笔记，以免时间长了忘记。</p><h2 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h2><h3 id="Blob"><a href="#Blob" class="headerlink" title="Blob"></a>Blob</h3><blockquote><p>Blob对象表示不可变的类似文件对象的原始数据。Blob表示不一定是JavaScript原生形式的数据。File接口基于Blob，继承了blob的功能并将其扩展使其支持用户系统的文件。</p><p>摘自MDN</p></blockquote><p>和<strong>File对象</strong>的区别</p><p>File对象就是一个文件，当我们使用<code>input type=&quot;file&quot;</code>标签来上传文件，我们在代码里得到的对象就是一个File对象。</p><p>Blob对象就是二进制数据，用来包装二进制文件的容器，可以通过<code>new Blob()</code>创建的对象就是Blob对象。又比如，在XMLHttpRequest里，如果指定responseType为blob，那么得到的返回值也是一个blob对象。</p><p>File继承于Blob</p><h3 id="fileReader"><a href="#fileReader" class="headerlink" title="fileReader"></a>fileReader</h3><blockquote><p>fileReader对象允许Web应用程序异步读取存储在计算机上的文件（原始数据缓冲区）的内容，使用File或Blob对象指定要读取的文件或数据。</p><p>摘自MDN</p></blockquote><p><code>FileReader</code>是用来读取内存中的文件的API，支持<code>File</code>和<code>Blob</code>两种格式。</p><h2 id="转化关系"><a href="#转化关系" class="headerlink" title="转化关系"></a>转化关系</h2><p>这边从使用场景出发，来整理一下。</p><h3 id="file转化为DataURL"><a href="#file转化为DataURL" class="headerlink" title="file转化为DataURL"></a>file转化为DataURL</h3><p><strong>场景</strong>： 获取到一个file类型的图片，直接在页面中预览。如果等图片上传完服务器返回图片的地址，再赋值给img的src属性，无疑浪费了时间和带宽。如果直接在html中预览？这里就是利用html5的新特性，将图片转换为Base64的形式显示出来。</p><p>下面的方法可以用于在浏览器上预览本地图片或者视频。</p><h4 id="方法1：URL-createObjectURL"><a href="#方法1：URL-createObjectURL" class="headerlink" title="方法1：URL.createObjectURL"></a>方法1：URL.createObjectURL</h4><p>该方法会根据传入的参数创建一个指向该参数对象的URL，这个URL是一个基于当前文件并且存储在内存中的<code>URL</code>，直到document触发了unload事件或者执行revokeObjectURL来释放。</p><p>可以用于在浏览器上预览本地图片或者视频</p><p><strong>使用方法如下</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">objectURL = URL.createObjectURL(param);</span><br></pre></td></tr></table></figure><ul><li>参数：可以为File或者Blob对象</li><li>返回：一段带hash的url，类似<code>blob:null/2c5e93e3-f06e-4bf2-8942-1708759bf7a7</code></li></ul><p><strong>注意</strong>：在每次调用 <code>createObjectURL()</code>方法时，都会创建一个新的 URL 对象。当不再需要这些 URL 对象时，每个对象必须通过调用<code>URL.revokeObjectURL()</code>方法来释放，让浏览器知道这个URL已经不再需要指向对应的文件。<code>URL.revokeObjectURL()</code>方法会释放一个通过<code>URL.createObjectURL()</code>创建的对象URL。浏览器关闭后自动释放这个对象，但是为了获得最佳性能和内存使用状况，你应该在安全的时机主动释放掉它们。</p><p>代码如下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> img = <span class="built_in">document</span>.createElement(<span class="string">"img"</span>);</span><br><span class="line">img.src = <span class="built_in">window</span>.URL.createObjectURL(files[i]);</span><br><span class="line">img.width = <span class="number">200</span>;</span><br><span class="line">img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line"><span class="built_in">window</span>.URL.revokeObjectURL(<span class="keyword">this</span>.src);</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">document</span>.body.appendChild(img);</span><br></pre></td></tr></table></figure><h4 id="方法2：FileReader-readAsDataURL"><a href="#方法2：FileReader-readAsDataURL" class="headerlink" title="方法2：FileReader.readAsDataURL"></a>方法2：FileReader.readAsDataURL</h4><p>该方法会读取指定的 Blob 或 File 对象。读取操作完成的时候，readyState 会变成已完成（DONE），并触发 loadend 事件，同时 result 属性将包含一个data:URL格式的字符串（base64编码）以表示所读取文件的内容。</p><p><strong>使用方法如下</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">FileReader.readAsDataURL(blob);</span><br></pre></td></tr></table></figure><ul><li>参数：可以为File或者Blob对象</li><li>返回：图片的base64编码。类似<code>data:image/jpeg;base64,/9j/4AAQSkZJRgABAQEASABIA</code></li></ul><p>代码如下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> reader = <span class="keyword">new</span> FileReader();</span><br><span class="line">reader.onload = <span class="function"><span class="keyword">function</span>(<span class="params">e</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// result属性是包含data:URL格式的字符串（base64编码）表示读取的文件内容</span></span><br><span class="line">  img.src = e.target.result;</span><br><span class="line">  img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    dosomething()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 读取file对象</span></span><br><span class="line">reader.readAsDataURL(files[i]);</span><br></pre></td></tr></table></figure><h4 id="两种方法的区别"><a href="#两种方法的区别" class="headerlink" title="两种方法的区别"></a>两种方法的区别</h4><p>1、执行时机</p><ul><li><code>createObjectURL</code>是同步执行（立即的）</li><li><code>FileReader.readAsDataURL</code>是异步执行</li></ul><p>2、内存使用</p><ul><li><code>createObjectURL</code>返回一段带<code>hash</code>的<code>url</code>，并且一直存储在内存中。</li><li><code>FileReader.readAsDataURL</code>则返回包含很多字符的<code>base64</code>，并会比<code>blob url</code>消耗更多内存，但是在不用的时候会自动从内存中清除（通过垃圾回收机制）</li></ul><p>3、兼容性</p><ul><li><code>createObjectURL</code>支持从IE10往上的所有现代浏览器</li><li><code>FileReader.readAsDataURL</code>同样支持从IE10往上的所有现代浏览器</li></ul><p>从上面答案不难看出，两者的优劣势</p><ul><li><p>使用<code>createObjectURL</code>可以节省性能并更快速，只不过需要在不使用的情况下手动释放内存</p></li><li><p>如果不太在意设备性能问题，并想获取图片的<code>base64</code>，则推荐使用</p></li></ul><h2 id="canvas-转化为DataURL"><a href="#canvas-转化为DataURL" class="headerlink" title="canvas 转化为DataURL"></a>canvas 转化为DataURL</h2><p><strong>场景</strong>： canvas画出来的图片，需要在其它地方预览或者作为组件之间通信的参数。</p><p><strong>使用方法如下</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.toDataURL(type, encoderOptions);</span><br></pre></td></tr></table></figure><ul><li>参数：<ul><li>type：图片格式，默认为 <code>image/png</code></li><li>encoderOptions：在指定图片格式为 <code>image/jpeg</code>或<code>image/webp</code>的情况下，可以从 0 到 1 的区间内选择图片的质量。如果超出取值范围，将会使用默认值 <code>0.92</code>。其他参数会被忽略。</li></ul></li><li>返回：图片的base64编码。类似<code>data:image/jpeg;base64,/9j/4AAQSkZJRgABAQEASABIA</code></li></ul><p>使用</p><ul><li>当一个内容画到canvas上时，我们可以将它生成任何一个格式支持的图片文件。</li><li>这个方法也可以用来对图片进行格式转化。比如，file(png格式) -&gt; canvas -&gt; base64(webp格式)，参考下图。</li><li>这个方法还可以用来压缩图片。</li></ul><p><img src="https://ws3.sinaimg.cn/large/006tKfTcly1g1jvojadcqj30b702kjrn.jpg" alt=""></p><h2 id="canvas-转化为blob"><a href="#canvas-转化为blob" class="headerlink" title="canvas 转化为blob"></a>canvas 转化为blob</h2><p><strong>场景</strong>： canvas画出来的图片，或者利用canvas完成截图功能之后，我们需要把对应的文件上传到服务器。</p><p><strong>使用方法如下</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">canvas.toBlob(callback, type, encoderOptions);</span><br></pre></td></tr></table></figure><ul><li>参数：<ul><li>callback：回调函数，可获得一个单独的<code>Blob</code>对象参数</li><li>type：<code>DOMString</code>类型，指定图片格式，默认格式为<code>image/png</code></li><li>encoderOptions：<code>Number</code>类型，值在0与1之间，当请求图片格式为<code>image/jpeg或者``image/webp时用来指定图片展示质量。如果这个参数的值不在指定类型与范围之内，则使用默认值，其余参数将被忽略。</code></li></ul></li><li>返回：无返回值</li></ul><p>得到Blob对象以后，可以利用XML2的<code>FormData</code>对象上传，模拟表单控件，异步上传这个二进制文件。</p><p>代码如下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">canvas.toBlob &amp;&amp; canvas.toBlob(<span class="function"><span class="keyword">function</span>(<span class="params">blob</span>) </span>&#123;</span><br><span class="line">　<span class="keyword">let</span> myForm = <span class="keyword">new</span> FormData();</span><br><span class="line">myForm.append(<span class="string">'image'</span>, blob); <span class="comment">//向表单中添加一个键值对</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="DataURL或img转canvas"><a href="#DataURL或img转canvas" class="headerlink" title="DataURL或img转canvas"></a>DataURL或img转canvas</h2><p><strong>场景</strong>： 想要对图片想要进行一些操作，比如加蒙层，修饰，裁剪等等，这些操作可以利用canvas的特性去完成。</p><p><strong>使用方法如下</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">context.drawImage(img,sx,sy,swidth,sheight,x,y,width,height);</span><br></pre></td></tr></table></figure><ul><li>参数<ul><li>image：绘制到上下文的元素。</li><li>sx：需要绘制到目标上下文中的，<code>image</code>的矩形（裁剪）选择框的左上角 X 轴坐标。</li><li>sy：需要绘制到目标上下文中的，<code>image</code>的矩形（裁剪）选择框的左上角 Y 轴坐标。</li><li>swidth：需要绘制到目标上下文中的，<code>image</code>的矩形（裁剪）选择框的宽度。如果不说明，整个矩形（裁剪）从坐标的<code>sx</code>和<code>sy</code>开始，到<code>image</code>的右下角结束。</li><li>sheight：需要绘制到目标上下文中的，<code>image</code>的矩形（裁剪）选择框的高度。</li></ul></li></ul><p><img src="https://mdn.mozillademos.org/files/225/Canvas_drawimage.jpg" alt="drawImage"></p><p>代码如下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">dataURLToCanvas</span>(<span class="params">dataurl</span>)</span>&#123;</span><br><span class="line"><span class="keyword">let</span> canvas = <span class="built_in">document</span>.createElement(<span class="string">'canvas'</span>);</span><br><span class="line"><span class="keyword">let</span> ctx = canvas.getContext(<span class="string">'2d'</span>);</span><br><span class="line"><span class="keyword">let</span> img = <span class="keyword">new</span> Image();</span><br><span class="line">img.onload = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">canvas.width = img.width;</span><br><span class="line">canvas.height = img.height;</span><br><span class="line">ctx.drawImage(img, <span class="number">0</span>, <span class="number">0</span>);</span><br><span class="line">&#125;;</span><br><span class="line">img.src = dataurl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      
      
        &lt;p&gt;近在做图片上传，接触了一些图片相关的转化操作。主要涉及blob，canvas，dataurl。做一下笔记，以免时间长了忘记。&lt;/p&gt;
&lt;h2 id=&quot;名词解释&quot;&gt;&lt;a href=&quot;#名词解释&quot; class=&quot;headerlink&quot; title=&quot;名词解释&quot;&gt;&lt;/a&gt;名词解释
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="https://github.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>前端性能监控</title>
    <link href="https://github.com/2019/02/12/%E5%89%8D%E7%AB%AF%E6%80%A7%E8%83%BD%E7%9B%91%E6%8E%A7/"/>
    <id>https://github.com/2019/02/12/%E5%89%8D%E7%AB%AF%E6%80%A7%E8%83%BD%E7%9B%91%E6%8E%A7/</id>
    <published>2019-02-12T07:50:28.691Z</published>
    <updated>2019-11-22T06:42:54.025Z</updated>
    
    <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>前端性能关系到页面的用户体验，页面的用户体验影响了产品的留存率，而产品则直接决定了一个公司的生死存亡。</p><p>Global Web Performance Matters for ecommerce的报告中指出：</p><p><img src="https://ws1.sinaimg.cn/large/006tNc79ly1g26v7nngpij30ys0ligo1.jpg" alt=""></p><ul><li><p>57%的用户更在乎网页在3秒内是否完成加载。</p></li><li><p>52%的在线用户认为网页打开速度影响到他们对网站的忠实度。</p></li><li><p>每慢1秒造成页面 PV 降低11%，用户满意度也随之降低降低16%。</p></li><li><p>近半数移动用户因为在10秒内仍未打开页面从而放弃。</p></li></ul><p>在我们做前端性能优化的时候，我们必须知道我们所做的优化到底能提升多少性能，这时候就有必要对前端性能进行监控。当然，我们现在这边讨论的前端性能，是关于页面加载和延迟方面的性能，暂时不讨论对错误处理的相关内容。</p><h2 id="Performance"><a href="#Performance" class="headerlink" title="Performance"></a>Performance</h2><p>首先简单介绍一下前端相关的性能指标，我们关注的几个指标如下。</p><ul><li>白屏时间 = 地址栏输入URL回车 - 浏览器出现第一个元素</li><li>首屏时间 = 地址栏输入URL回车 - 浏览器第一屏渲染完成</li><li>用户可操作时间</li><li>页面总下载时间</li></ul><p><img src="https://ws4.sinaimg.cn/large/006tNc79ly1g26mno2un2j30sx08l0uw.jpg" alt=""></p><p>对开发人员而言，我们常用的性能指标API就是Performance。Performance 接口可以获取到当前页面与性能相关的信息。</p><p>我们使用<code>window.performance</code>就可以获得 performance对象。</p><p>在控制台我们打印performance，可以看到performance对象包含五个属性。</p><ul><li><p>navigation 提供了在指定的时间段里发生的操作相关信息，包括页面是加载还是刷新、发生了多少次重定向等等。</p></li><li><p>timing 对象包含延迟相关的性能信息，也是这次我们需要</p></li><li><p>memory 对象</p><ul><li>jsHeapSizeLimit: 内存大小限制</li><li>totalJSHeapSize: 可使用的内存</li><li>usedJSHeapSize: JS 对象占用的内存</li></ul></li><li><p>timeOrigin 返回性能测量开始时的时间的高精度时间戳</p></li></ul><p><img src="https://ws2.sinaimg.cn/large/006tNc79ly1g26ndj4g0rj30jf0f9ju2.jpg" alt=""></p><p>这些字段的含义与浏览器对应的状态如下</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g26n2g4tg3j30m80cygpg.jpg" alt=""></p><p>如图，以responseEnd为分界点。图中的红线的左半部分代表网络传输层面，右半部分代表浏览器解析和渲染相关相关。</p><p>我这边先给出上述字段的含义，个人认为比较重要的节点加粗了</p><ul><li><strong>navigationStart: 表示从上一个文档卸载结束时的 unix 时间戳，如果没有上一个文档，这个值将和 fetchStart 相等</strong>。</li><li>unloadEventStart: 表示前一个网页（与当前页面同域）unload 的时间戳，如果无前一个网页 unload 或者前一个网页与当前页面不同域，则值为 0。</li><li>unloadEventEnd: 返回前一个页面 unload 时间绑定的回掉函数执行完毕的时间戳。</li><li>redirectStart: 第一个 HTTP 重定向发生时的时间。有跳转且是同域名内的重定向才算，否则值为 0。</li><li>redirectEnd: 最后一个 HTTP 重定向完成时的时间。有跳转且是同域名内部的重定向才算，否则值为 0。</li><li><strong>fetchStart: 浏览器准备好使用 HTTP 请求抓取文档的时间，这发生在检查本地缓存之前</strong>。</li><li>domainLookupStart/domainLookupEnd: DNS 域名查询开始/结束的时间，如果使用了本地缓存（即无 DNS 查询）或持久连接，则与 fetchStart 值相等</li><li>connectStart: HTTP（TCP）开始/重新 建立连接的时间，如果是持久连接，则与 fetchStart 值相等。</li><li>connectEnd: HTTP（TCP） 完成建立连接的时间（完成握手），如果是持久连接，则与 fetchStart 值相等。</li><li>secureConnectionStart: HTTPS 连接开始的时间，如果不是安全连接，则值为 0。</li><li>requestStart: HTTP 请求读取真实文档开始的时间（完成建立连接），包括从本地读取缓存。</li><li><strong>responseStart: HTTP 开始接收响应的时间（获取到第一个字节），包括从本地读取缓存</strong>。</li><li><strong>responseEnd: HTTP 响应全部接收完成的时间（获取到最后一个字节），包括从本地读取缓存</strong>。</li><li>domLoading: 开始解析渲染 DOM 树的时间，此时 Document.readyState 变为 loading，并将抛出 readystatechange 相关事件。</li><li>domInteractive: 完成解析 DOM 树的时间，Document.readyState 变为 interactive，并将抛出 readystatechange 相关事件，注意只是 DOM 树解析完成，这时候并没有开始加载网页内的资源。</li><li><strong>domContentLoadedEventStart: DOM 解析完成后，网页内资源加载开始的时间，在 DOMContentLoaded 事件抛出前发生。</strong></li><li><strong>domContentLoadedEventEnd: DOM 解析完成后，网页内资源加载完成的时间（如 JS 脚本加载执行完毕）。</strong></li><li>domComplete: DOM 树解析完成，且资源也准备就绪的时间，Document.readyState 变为 complete，并将抛出 readystatechange 相关事件。</li><li><strong>loadEventStart: load 事件发送给文档，也即 load 回调函数开始执行的时间。</strong></li><li><strong>loadEventEnd: load 事件的回调函数执行完毕的时间。</strong></li></ul><p>(1) DOMContentLoaded 是指页面元素加载完毕，但是一些资源比如图片还无法看到，但是这个时候页面是可以正常交互的，比如滚动，输入字符等。 jQuery 中经常使用的 <code>$(document).ready()</code> 其实监听的就是 DOMContentLoaded 事件。</p><p>(2) load 是指页面上所有的资源（图片，音频，视频等）加载完成。jQuery 中 <code>$(document).load()</code> 监听的是 load 事件。</p><h2 id="实战测试"><a href="#实战测试" class="headerlink" title="实战测试"></a>实战测试</h2><p>由于上面的一些字段解释源于网上的文档和资料，所以我自己需要做一些代码上的测试。</p><h4 id="一、-domContentLoadedEventStart-和-domContentLoadedEventEnd-区别"><a href="#一、-domContentLoadedEventStart-和-domContentLoadedEventEnd-区别" class="headerlink" title="一、 domContentLoadedEventStart 和 domContentLoadedEventEnd 区别"></a>一、 <strong>domContentLoadedEventStart</strong> 和 <strong>domContentLoadedEventEnd</strong> 区别</h4><p>代码如下，我们计算了<strong>DOMContentLoaded</strong>的回调执行时间。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">'DOMContentLoaded'</span>, () =&gt; &#123;</span><br><span class="line">  <span class="built_in">console</span>.time(<span class="string">'DOMContentLoaded'</span>)</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>;i&lt;<span class="number">1000000000</span>;i++)&#123;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">console</span>.timeEnd(<span class="string">'DOMContentLoaded'</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p>在浏览器控制台，我们打印了这两个字段的差值（时间差）。</p><p><img src="https://ws4.sinaimg.cn/large/006tNc79ly1g26sdstdhyj30f602rglq.jpg" alt=""></p><p>结论：两个字段的差值刚好等于<strong>DOMContentLoaded</strong>的回调执行时间，网上关于字段的解释正确。并且，真正触发<strong>DOMContentLoaded</strong>事件应该是<strong>domContentLoadedEventStart</strong>对应的时间。</p><h4 id="二、DOMContentLoaded-是对应domContentLoadedEventStart-和-domContentLoadedEventEnd-哪个字段。"><a href="#二、DOMContentLoaded-是对应domContentLoadedEventStart-和-domContentLoadedEventEnd-哪个字段。" class="headerlink" title="二、DOMContentLoaded 是对应domContentLoadedEventStart 和 domContentLoadedEventEnd 哪个字段。"></a>二、DOMContentLoaded 是对应<strong>domContentLoadedEventStart</strong> 和 <strong>domContentLoadedEventEnd</strong> 哪个字段。</h4><p>在浏览器控制台，我们打印了它和<strong>navigationStart</strong>的差值。</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g26sg5cw81j30i0036t8z.jpg" alt=""></p><p>在浏览器network的下面，我们看到显示的DOMContentLoaded时间是1.41秒</p><p><img src="https://ws4.sinaimg.cn/large/006tNc79ly1g26sgn7ggej30jb09mt9p.jpg" alt=""></p><p>结论：浏览器上的<strong>DOMContentLoaded</strong>的时间实际代表的是<strong>domContentLoadedEventEnd</strong> 的时间。</p><p>####三、 当前网页load事件还没有发生，返回0。</p><p>在<strong>DOMContentLoaded</strong>事件的回调里，我们发现<strong>loadEventStart</strong>和<strong>loadEventEnd</strong>都是0。</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g26t549skdj30r60gx42w.jpg" alt=""></p><p>在load事件的回调里，我们发现loadEventStart有值，loadEventEnd是0。</p><p><img src="https://ws4.sinaimg.cn/large/006tNc79ly1g26t8gosztj30op0f8gpe.jpg" alt=""></p><p>结论：如果需要计算load的时间的话，一定要注意代码在页面load事件触发以后再去计算。</p><h2 id="性能指标的计算"><a href="#性能指标的计算" class="headerlink" title="性能指标的计算"></a>性能指标的计算</h2><h4 id="一、白屏时间"><a href="#一、白屏时间" class="headerlink" title="一、白屏时间"></a>一、白屏时间</h4><p>再明确一下白屏时间的定义，用户从打开页面开始到页面开始有东西呈现为止。</p><p><strong>如果是chrome高版本，直接可以通过下面代码获得</strong></p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">// 白屏时间</span><br><span class="line">(chrome.loadTimes().firstPaintTime - chrome.loadTimes().startLoadTime)*1000;</span><br></pre></td></tr></table></figure><p><strong>如果是没有该API的普通浏览器</strong></p><p>我们通常认为浏览器开始渲染 <code>&lt;body&gt;</code> 或者解析完 <code>&lt;head&gt;</code> 的时间是白屏结束的时间点。</p><p>所以，白屏时间 = 开始渲染时间 + 头部资源加载时间</p><h5 id="1、开始渲染时间"><a href="#1、开始渲染时间" class="headerlink" title="1、开始渲染时间"></a>1、开始渲染时间</h5><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//开始渲染时间</span></span><br><span class="line">startTime = performance.timing.domLoading - performance.timing.navigationStart;</span><br></pre></td></tr></table></figure><h5 id="2、头部资源加载时间"><a href="#2、头部资源加载时间" class="headerlink" title="2、头部资源加载时间"></a>2、头部资源加载时间</h5><p>我们在<code>&lt;head&gt;</code>的前面计时开始，在<code>&lt;head&gt;</code>最末尾计时结束，中间的差值就是头部资源加载时间。</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width,initial-scale=1"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">      <span class="keyword">var</span> start_time = <span class="keyword">new</span> <span class="built_in">Date</span>; <span class="comment">//测试时间起点，实际统计起点为 DNS 查询</span></span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">for</span>(<span class="keyword">let</span> i=<span class="number">0</span>; i&lt;<span class="number">10000000000</span>; i++)&#123;</span></span><br><span class="line">    &#125;</span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 页面 CSS 资源 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"xx.css"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    <span class="keyword">var</span> end_time = +<span class="keyword">new</span> <span class="built_in">Date</span>; <span class="comment">//时间终点</span></span></span><br><span class="line"><span class="actionscript">    <span class="keyword">var</span> headTime = end_time - start_time; <span class="comment">//头部资源加载时间</span></span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure><p>所以最终，白屏时间 =  开始渲染时间(startTime) + 头部资源加载时间(headTime)</p><h4 id="二、首屏时间"><a href="#二、首屏时间" class="headerlink" title="二、首屏时间"></a>二、首屏时间</h4><p>再明确一下首屏时间的定义，用户浏览器首屏内所有内容都呈现出来所花费的时间。</p><p>影响首屏的主要因素是图片的加载。基本流程如下</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">首屏位置调用 API 开始统计 -&gt; 绑定首屏内所有图片的 load 事件 -&gt; 页面加载完后判断图片是否在首屏内，找出加载最慢的一张 -&gt; 首屏时间</span><br></pre></td></tr></table></figure><p>对于网页高度小于屏幕的网站来说，只要在页面底部加上脚本打印当前时间即可；或者对于网页高度大于一屏的网页来说，只要在估算接近于一屏幕的元素的位置后，打印一下当前时间。当然这个时间要得把首屏中所有图片的加载时间也算上。</p><h4 id="三、用户可操作时间"><a href="#三、用户可操作时间" class="headerlink" title="三、用户可操作时间"></a>三、用户可操作时间</h4><p><strong>用户可操作时间</strong>影响交互体验，通常我们把所有事件的绑定都放在domReady以后，所以用户可操作的时间可以等于DOM解析完毕的时间。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">用户可操作时间 = performance.timing.domInteractive - performance.timing.navigationStart</span><br></pre></td></tr></table></figure><h4 id="四、页面总下载时间"><a href="#四、页面总下载时间" class="headerlink" title="四、页面总下载时间"></a>四、页面总下载时间</h4><p><strong>页面总下载时间</strong>可以统计<strong>onload</strong>时间，load事件必须等到页面内包括图片的所有元素加载完毕后才能触发。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">页面总下载时间 = performance.timing.loadEventEnd - performance.timing.navigationStart</span><br></pre></td></tr></table></figure><h4 id="五、常见的指标"><a href="#五、常见的指标" class="headerlink" title="五、常见的指标"></a>五、常见的指标</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 计算加载时间</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getPerformanceTiming</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> t = performance.timing</span><br><span class="line">    <span class="keyword">var</span> times = &#123;&#125;</span><br><span class="line">    <span class="comment">// 页面加载完成的时间，用户等待页面可用的时间</span></span><br><span class="line">    times.loadPage = t.loadEventEnd - t.navigationStart</span><br><span class="line">    <span class="comment">// 解析 DOM 树结构的时间</span></span><br><span class="line">    times.domReady = t.domComplete - t.responseEnd</span><br><span class="line">    <span class="comment">// 重定向的时间</span></span><br><span class="line">    times.redirect = t.redirectEnd - t.redirectStart</span><br><span class="line">    <span class="comment">// DNS 查询时间</span></span><br><span class="line">    times.lookupDomain = t.domainLookupEnd - t.domainLookupStart</span><br><span class="line">    <span class="comment">// 读取页面第一个字节的时间</span></span><br><span class="line">    times.ttfb = t.responseStart - t.navigationStart</span><br><span class="line">    <span class="comment">// 资源请求加载完成的时间</span></span><br><span class="line">    times.request = t.responseEnd - t.requestStart</span><br><span class="line">    <span class="comment">// 执行 onload 回调函数的时间</span></span><br><span class="line">    times.loadEvent = t.loadEventEnd - t.loadEventStart</span><br><span class="line">    <span class="comment">// DNS 缓存时间</span></span><br><span class="line">    times.appcache = t.domainLookupStart - t.fetchStart</span><br><span class="line">    <span class="comment">// 卸载页面的时间</span></span><br><span class="line">    times.unloadEvent = t.unloadEventEnd - t.unloadEventStart</span><br><span class="line">    <span class="comment">// TCP 建立连接完成握手的时间</span></span><br><span class="line">    times.connect = t.connectEnd - t.connectStart</span><br><span class="line">    <span class="keyword">return</span> times</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="性能工具"><a href="#性能工具" class="headerlink" title="性能工具"></a>性能工具</h2><p>页面性能的评估与监控有很多工具，下面简单介绍几个常用的工具：</p><ul><li><p>Page Speed: 谷歌开发的分析和优化网页的工具，可以作为浏览器插件使用　　</p></li><li><p>WebPageTest：一款非常优秀的网页前端性能测试工具。使用 WebPagetest，你可以详细掌握网站加载过程中的瀑布流、性能得分、元素分布、视图分析等数据。其中比较直观的视图分析功能可以直接看到页面加载各个阶段的截屏。国内也有利用 WebPagetest 搭建的性能测试平台，推荐使用阿里测。　</p></li><li><p>PhantomJS：自动化监测，模拟Phantom JS 是一个服务器端的 JavaScript API 的 WebKit，基于它可以轻松实现 web 自动化测试。类似的有berserkJS。但是都是服务器模拟测试，不能监控用户真实环境。</p></li></ul>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h2&gt;&lt;p&gt;前端性能关系到页面的用户体验，页面的用户体验影响了产品的留存率，而产品则直接决定了一个公司的生死存亡。&lt;/p&gt;
&lt;p&gt;Global Web 
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="性能优化" scheme="https://github.com/tags/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>容易混淆的元素位置和大小属性</title>
    <link href="https://github.com/2019/02/12/%E5%AE%B9%E6%98%93%E6%B7%B7%E6%B7%86%E7%9A%84%E5%85%83%E7%B4%A0%E4%BD%8D%E7%BD%AE%E5%92%8C%E5%A4%A7%E5%B0%8F%E5%B1%9E%E6%80%A7/"/>
    <id>https://github.com/2019/02/12/%E5%AE%B9%E6%98%93%E6%B7%B7%E6%B7%86%E7%9A%84%E5%85%83%E7%B4%A0%E4%BD%8D%E7%BD%AE%E5%92%8C%E5%A4%A7%E5%B0%8F%E5%B1%9E%E6%80%A7/</id>
    <published>2019-02-12T07:50:28.691Z</published>
    <updated>2019-11-22T06:44:02.561Z</updated>
    
    <content type="html"><![CDATA[<h2 id="关于元素的大小"><a href="#关于元素的大小" class="headerlink" title="关于元素的大小"></a>关于元素的大小</h2><h3 id="关于尺寸的属性"><a href="#关于尺寸的属性" class="headerlink" title="关于尺寸的属性"></a>关于尺寸的属性</h3><ul><li><p>clientHeight和clientWidth：</p><ul><li><p>元素可视内容区域的高度和宽度，不包括水平滚动条、边框和外边距。比如，clientWidth=width+padding</p></li><li><p>单位是像素px。整数（亲测，这是个四舍五入后的值）。返回值不带px</p></li><li><p>只读属性</p><p><img src="https://developer.mozilla.org/@api/deki/files/185/=Dimensions-client.png" alt="image_1d03q576g1konsnt5pn8q53099.png-8.5kB"></p></li></ul></li><li><p>offsetHeight和offsetWidth：</p><ul><li><p>元素可视内容区域的高度和宽度，包括元素的边框、内边距和元素的水平滚动条（如果存在且渲染的话），不包括外边距和:before或:after等伪类元素的高度。比如，offsetWidth=width+padding+borderWidth</p></li><li><p>单位是像素px。整数（亲测，这是个四舍五入后的值）。返回值不带px</p></li><li><p>只读属性</p><p><img src="https://mdn.mozillademos.org/files/347/Dimensions-offset.png" alt="image_1d03q576g1konsnt5pn8q53099.png-8.5kB"></p></li></ul></li><li><p>scrollHeight 和 scrollWidth：</p><ul><li>元素内容的实际高度和宽度，包括由于溢出导致的视图中不可见的内容，也包括:before或:after等伪类元素的高度和宽度，不包括边框和外边距。没有垂直滚动条的情况下，scrollHeight与clientHeight相等。</li><li>单位是像素px。整数（亲测，这是个四舍五入后的值）。返回值不带px</li><li>只读属性</li></ul></li></ul><p><img src="https://developer.mozilla.org/@api/deki/files/840/=ScrollHeight.png" alt="image_1d03q576g1konsnt5pn8q53099.png-8.5kB"></p><h3 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h3><h4 id="一-获取视口和页面的大小"><a href="#一-获取视口和页面的大小" class="headerlink" title="一 获取视口和页面的大小"></a>一 获取视口和页面的大小</h4><p>网页大小：一张网页的全部面积，包括不可见的滚动内容，这个大小由代码设置。</p><p>浏览器大小：浏览器窗口看到的面积，也叫视口。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 视口大小</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getViewport</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">document</span>.compatMode == <span class="string">'BackCompat'</span>) &#123;    <span class="comment">// 文档模式判断</span></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      width: <span class="built_in">document</span>.body.clientWidth,</span><br><span class="line">      height: <span class="built_in">document</span>.body.clientHeight</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      width: <span class="built_in">document</span>.documentElement.clientWidth,</span><br><span class="line">      height: <span class="built_in">document</span>.documentElement.clientHeight</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 网页大小</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getPagearea</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">document</span>.compatMode == <span class="string">'BackCompat'</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      width: <span class="built_in">document</span>.body.scrollWidth,</span><br><span class="line">      height: <span class="built_in">document</span>.body.scrollHeight</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      width: <span class="built_in">document</span>.documentElement.scrollWidth,</span><br><span class="line">      height: <span class="built_in">document</span>.documentElement.scrollHeight</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="二-判断元素是否滚动到底"><a href="#二-判断元素是否滚动到底" class="headerlink" title="二 判断元素是否滚动到底"></a>二 判断元素是否滚动到底</h4><p>如果元素已经滚动到底，则它的实际内容高度减去已滚动的距离应该等于可视区域高度。下面等式返回true代表已经到底，反之则没有。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">element.scrollHeight - element.scrollTop === element.clientHeight</span><br></pre></td></tr></table></figure><h2 id="关于元素的位置"><a href="#关于元素的位置" class="headerlink" title="关于元素的位置"></a>关于元素的位置</h2><h3 id="关于位置的属性"><a href="#关于位置的属性" class="headerlink" title="关于位置的属性"></a>关于位置的属性</h3><ul><li><p>offsetTop和offsetLeft：</p><ul><li><p>元素距离offsetParent的高度。offsetParent指的是距离该元素最近的position不为static的祖先元素。如果没有则指向body元素。</p></li><li><p>单位是像素px。整数（亲测，这是个四舍五入后的值）。返回值不带px</p></li><li><p>只读属性</p><p><img src="http://static.zybuluo.com/only-twj520Q/2ecr3tew37lqeoauetihold1/image_1d03q6gme1li1igs1i2dllg15lqm.png" alt="image_1d03q6gme1li1igs1i2dllg15lqm.png-10.9kB"></p></li></ul></li><li><p>scrollTop 和 scrollLeft：</p><ul><li><p>元素的滚动距离。比如，scrollTop代表滚动条向下滚动的距离，也是是元素顶部被遮住的距离。在没有滚动条的时候，scrollTop等于0。</p></li><li><p>单位是像素px。整数（亲测，这是个四舍五入后的值）。返回值不带px</p></li><li><p>可写属性：如果设置小于0，它的值会等于0。如果设置超过了这个容器可滚动的距离，它的值会等于最大值</p><p><img src="https://developer.mozilla.org/@api/deki/files/842/=ScrollTop.png" alt="image_1d03q6gme1li1igs1i2dllg15lqm.png-10.9kB"></p></li></ul></li></ul><h3 id="应用-1"><a href="#应用-1" class="headerlink" title="应用"></a>应用</h3><h4 id="一-获取元素绝对位置"><a href="#一-获取元素绝对位置" class="headerlink" title="一 获取元素绝对位置"></a>一 获取元素绝对位置</h4><p>绝对位置，指元素的左上角相对于整张网页左上角的坐标，通过计算获得。每个元素都有 offsetTop 和 offsetLeft 属性，表示该元素的左上角与父容器（offsetParent对象）左上角的距离，因此进行迭代累加，就可以得到该元素的绝对坐标。注意，此方面不适用于 表格 和 iframe，因为在其中，offsetParent 对象未必等于父容器。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getElementTop</span>(<span class="params">element</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> actualTop = element.offsetTop;</span><br><span class="line">  <span class="keyword">let</span> current = element.offsetParent;</span><br><span class="line">  <span class="keyword">while</span> (current !== <span class="literal">null</span>) &#123;</span><br><span class="line">actualTop += current.offsetTop;</span><br><span class="line">current = current.offsetParent;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> actualTop;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意: <strong>所有这些偏移量属性都是只读的</strong>，而且每次访问它们都需要重新计算。因此，应该尽量避免重复访问这些属性；如果需要重复使用其中某些属性的值，可以将它们保存在局部变量中，以提高性能。</p><h2 id="样式相关"><a href="#样式相关" class="headerlink" title="样式相关"></a>样式相关</h2><h3 id="getBoundingClientRect"><a href="#getBoundingClientRect" class="headerlink" title="getBoundingClientRect"></a>getBoundingClientRect</h3><p>使用方法：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">rectObject = element.getBoundingClientRect();</span><br></pre></td></tr></table></figure><ul><li><p>返回元素的大小及其相对于视口的位置，返回值是一个对象，包含width, height, bottom, right, left, top, 六个属性。left, right, top, bottom都是元素（不包括margin）相对于视口的原点（视口的上边界和左边界）的距离。</p></li><li><p>单位是像素px。浮点值。返回值不带px</p></li><li><p>只读属性，返回浮点值。</p></li></ul><p>如下图，浏览器的支持程度还是非常高的。</p><p><img src="https://ws3.sinaimg.cn/large/006tKfTcly1g1dr2tztwij30zk0drwh8.jpg" alt=""></p><p>自己测试的结果是，这个方法获取的height和offsetHeight大小是一致的。</p><h3 id="element-style"><a href="#element-style" class="headerlink" title="element.style"></a>element.style</h3><p>使用方法：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">inlineStyle = element.style;</span><br></pre></td></tr></table></figure><ul><li>返回值是一个<code>CSSStyleDeclaration</code>对象，返回元素标签内的样式，局限性在于使用这个方法只能获取到元素内联样式中的属性值。其中，<code>CSSStyleDeclaration</code> 表示一个CSS属性键值对的集合</li><li>单位是像素px。字符串。返回值带px</li><li>可写属性</li></ul><p>可以这样手动操作单个元素的宽度：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Element.style.width = <span class="string">'100px'</span>;</span><br></pre></td></tr></table></figure><h3 id="getComputedStyle"><a href="#getComputedStyle" class="headerlink" title="getComputedStyle"></a>getComputedStyle</h3><p>使用方法：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">window.getComputedStyle(element, [pseudoElt]);</span><br></pre></td></tr></table></figure><p>第二个参数是指定一个要匹配的伪元素的字符串，例如“:before”。如果不需要可以为null。CSS属性值可以使用<code>getPropertyValue(propName)</code>API或直接索引到对象。差别在于getPropertyValue不必使用驼峰的写法。</p><p>例如</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">window</span>.getComputedStyle(ele, <span class="literal">null</span>).getPropertyValue(<span class="string">"background-color"</span>);</span><br><span class="line"><span class="built_in">window</span>.getComputedStyle(ele, <span class="literal">null</span>).backgroundColor;</span><br></pre></td></tr></table></figure><ul><li>返回值是一个<code>CSSStyleDeclaration</code>对象，返回一个可以获取当前元素所有最终使用的CSS属性值</li><li>单位是像素px。字符串。返回值带px</li><li>只读属性</li></ul><p>如下图，浏览器的支持程度还是非常高的。</p><p><img src="https://ws4.sinaimg.cn/large/006tKfTcly1g1dqm9iuk0j30z20d0gop.jpg" alt=""></p><h3 id="element-currentStyle"><a href="#element-currentStyle" class="headerlink" title="element.currentStyle"></a>element.currentStyle</h3><p>这个属性是IE浏览器的属性。语法与element.style类似，差别在于element.currentStyle返回的是元素当前应用的最终CSS属性值（包括外链CSS文件，页面中嵌入的<code>&lt;style&gt;</code>属性等）。</p><h3 id="应用-2"><a href="#应用-2" class="headerlink" title="应用"></a>应用</h3><h4 id="一-判断元素是否在可视区域"><a href="#一-判断元素是否在可视区域" class="headerlink" title="一 判断元素是否在可视区域"></a>一 判断元素是否在可视区域</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方法1</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isInViewPortOfTwo</span> (<span class="params">el</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> viewPortHeight = <span class="built_in">window</span>.innerHeight || <span class="built_in">document</span>.documentElement.clientHeight || <span class="built_in">document</span>.body.clientHeight </span><br><span class="line">  <span class="keyword">const</span> top = el.getBoundingClientRect() &amp;&amp; el.getBoundingClientRect().top</span><br><span class="line">  <span class="keyword">const</span> offsetTop = el.offsetTop</span><br><span class="line">  <span class="keyword">const</span> scrollTop = <span class="built_in">document</span>.documentElement.scrollTop</span><br><span class="line">  <span class="keyword">const</span> topGap = offsetTop - scrollTop</span><br><span class="line">  <span class="keyword">return</span> topGap  &lt;= viewPortHeight</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 方法2</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isInViewPortOfTwo</span> (<span class="params">el</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> viewPortHeight = <span class="built_in">window</span>.innerHeight || <span class="built_in">document</span>.documentElement.clientHeight || <span class="built_in">document</span>.body.clientHeight;</span><br><span class="line">  <span class="keyword">const</span> &#123; top, height &#125; = el.getBoundingClientRect();</span><br><span class="line">  <span class="keyword">return</span> (top &lt;= viewPortHeight) &amp;&amp; (top + height &gt;= <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="二-图片懒加载"><a href="#二-图片懒加载" class="headerlink" title="二 图片懒加载"></a>二 图片懒加载</h4><p>实现一个非常简易的图片懒加载</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;img </span><br><span class="line">  <span class="class"><span class="keyword">class</span></span>=<span class="string">"test-img"</span></span><br><span class="line">  src=<span class="string">""</span> </span><br><span class="line">  data-src=<span class="string">"httt://www.xxx.com/images/001.jpg"</span></span><br><span class="line">/&gt;</span><br></pre></td></tr></table></figure><p>监听scroll事件，当图片出现在可视区域时，提取data-src的值并赋给src，加载真正图片</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> img = <span class="built_in">document</span>.getElementsByClassName(<span class="string">'test-img'</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">//设置每次遍历的起始图片，防止重复加载</span></span><br><span class="line"><span class="keyword">let</span> n = <span class="number">0</span></span><br><span class="line"><span class="comment">//首次加载可视区域图片</span></span><br><span class="line">lazyLoad();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">lazyLoad</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = n; i&lt;img.length;i++) &#123;</span><br><span class="line">    <span class="keyword">let</span> el = img[i];</span><br><span class="line">    <span class="keyword">if</span>(el.getAttribute(<span class="string">'src'</span>) === <span class="string">''</span> &amp;&amp; isInViewPortOfTwo(el))&#123;</span><br><span class="line">      el.setAttribute(<span class="string">'src'</span>, el.getAttribute(<span class="string">'data-src'</span>))</span><br><span class="line">      n++;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 判断是否在可视范围内</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">isInViewPortOfTwo</span> (<span class="params">el</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> viewPortHeight = <span class="built_in">window</span>.innerHeight || <span class="built_in">document</span>.documentElement.clientHeight || <span class="built_in">document</span>.body.clientHeight;</span><br><span class="line">  <span class="keyword">const</span> top = el.getBoundingClientRect() &amp;&amp; el.getBoundingClientRect().top;</span><br><span class="line">  <span class="keyword">return</span> top  &lt;= viewPortHeight</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">window</span>.addEventListener(<span class="string">'scroll'</span>, _.debounce(lazyLoad, <span class="number">1000</span>))</span><br></pre></td></tr></table></figure><h4 id="三-获取元素的位置"><a href="#三-获取元素的位置" class="headerlink" title="三 获取元素的位置"></a>三 获取元素的位置</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 相对位置</span></span><br><span class="line"><span class="keyword">var</span> X= <span class="keyword">this</span>.getBoundingClientRect().left;</span><br><span class="line"><span class="keyword">var</span> Y =<span class="keyword">this</span>.getBoundingClientRect().top;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 绝对位置</span></span><br><span class="line"><span class="keyword">var</span> X= <span class="keyword">this</span>.getBoundingClientRect().left + <span class="built_in">document</span>.documentElement.scrollLeft;</span><br><span class="line"><span class="keyword">var</span> Y =<span class="keyword">this</span>.getBoundingClientRect().top  +<span class="built_in">document</span>.documentElement.scrollTop;</span><br></pre></td></tr></table></figure><p>  <strong>测试代码附上</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br></pre></td><td class="code"><pre><span class="line">&lt;!DOCTYPE html&gt;</span><br><span class="line">&lt;html&gt;</span><br><span class="line">&lt;head&gt;</span><br><span class="line">  &lt;style&gt;</span><br><span class="line">    * &#123;</span><br><span class="line">      padding: <span class="number">0</span>;</span><br><span class="line">      margin: <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    #app &#123;</span><br><span class="line">      border: 1px solid #333333;</span><br><span class="line">      margin-top: <span class="number">50</span>px;</span><br><span class="line">      padding-top: <span class="number">50</span>px;</span><br><span class="line">    &#125;</span><br><span class="line">    #box1 &#123;</span><br><span class="line">      width: <span class="number">100</span>px;</span><br><span class="line">      height: <span class="number">50</span>px;</span><br><span class="line">      padding: <span class="number">50</span>px;</span><br><span class="line">      border: 50px solid #000000;</span><br><span class="line">      background-color: #00D8C9;</span><br><span class="line">      font-size: <span class="number">20</span>px;</span><br><span class="line">      overflow: scroll;</span><br><span class="line">    &#125;</span><br><span class="line">    #box2 &#123;</span><br><span class="line">      width: <span class="number">200</span>px;</span><br><span class="line">      height: <span class="number">200</span>px;</span><br><span class="line">      margin-top: <span class="number">500</span>px;</span><br><span class="line">      border: 50px solid #FF4747;</span><br><span class="line">      background-color: #00D8C9;</span><br><span class="line">    &#125;</span><br><span class="line">  &lt;<span class="regexp">/style&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>head&gt;</span><br><span class="line">&lt;body&gt;</span><br><span class="line">  &lt;div id=<span class="string">"app"</span>&gt;</span><br><span class="line">    &lt;div id=<span class="string">"box1"</span>&gt;第一个盒子第一个盒子第一个盒子第一个盒子第一个盒子第一个盒子&lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">    &lt;div id="box2" style="width: 500px; height: 100px"&gt;第一个盒子&lt;/</span>div&gt;</span><br><span class="line">  &lt;<span class="regexp">/div&gt;</span></span><br><span class="line"><span class="regexp">&lt;script&gt;</span></span><br><span class="line"><span class="regexp">let box1 = document.getElementById('box1');</span></span><br><span class="line"><span class="regexp">let box2 = document.getElementById('box2');</span></span><br><span class="line"><span class="regexp">function getViewport() &#123;</span></span><br><span class="line"><span class="regexp">  /</span><span class="regexp">/ 文档模式判断</span></span><br><span class="line"><span class="regexp">  if (document.compatMode == 'BackCompat') &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      width: document.body.clientWidth,</span></span><br><span class="line"><span class="regexp">      height: document.body.clientHeight</span></span><br><span class="line"><span class="regexp">    &#125;;</span></span><br><span class="line"><span class="regexp">  &#125; else &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      width: document.documentElement.clientWidth,</span></span><br><span class="line"><span class="regexp">      height: document.documentElement.clientHeight</span></span><br><span class="line"><span class="regexp">    &#125;;</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">function getPagearea() &#123;</span></span><br><span class="line"><span class="regexp">  if (document.compatMode == 'BackCompat') &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      width: document.body.scrollWidth,</span></span><br><span class="line"><span class="regexp">      height: document.body.scrollHeight</span></span><br><span class="line"><span class="regexp">    &#125;;</span></span><br><span class="line"><span class="regexp">  &#125; else &#123;</span></span><br><span class="line"><span class="regexp">    return &#123;</span></span><br><span class="line"><span class="regexp">      width: document.documentElement.scrollWidth,</span></span><br><span class="line"><span class="regexp">      height: document.documentElement.scrollHeight</span></span><br><span class="line"><span class="regexp">    &#125;;</span></span><br><span class="line"><span class="regexp">  &#125;</span></span><br><span class="line"><span class="regexp">&#125;</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">console.log('视口宽度', getViewport().width)</span></span><br><span class="line"><span class="regexp">console.log('视口高度', getViewport().height)</span></span><br><span class="line"><span class="regexp">console.log('网页宽度', getPagearea().width)</span></span><br><span class="line"><span class="regexp">console.log('网页高度', getPagearea().height)</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">console.log('---------元素尺寸----------')</span></span><br><span class="line"><span class="regexp">console.log('第一个元素的高度，clientHeight', box1.clientHeight)</span></span><br><span class="line"><span class="regexp">console.log('第一个元素的高度，offsetHeight', box1.offsetHeight)</span></span><br><span class="line"><span class="regexp">console.log('第一个元素的高度，scrollHeight', box1.scrollHeight)</span></span><br><span class="line"><span class="regexp">console.log('第一个元素的高度，scrollTop', box1.scrollTop)</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">console.log('---------元素距离----------')</span></span><br><span class="line"><span class="regexp">console.log('第二个元素距离父元素的高度 offsetTop', box2.offsetTop)</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">console.log('---------元素样式----------')</span></span><br><span class="line"><span class="regexp">console.log('第二个元素的DOMRect getBoundingClientRect', box2.getBoundingClientRect())</span></span><br><span class="line"><span class="regexp"></span></span><br><span class="line"><span class="regexp">console.log('第二个元素的 style', box2.style)</span></span><br><span class="line"><span class="regexp">console.log('第二个元素的 getComputedStyle', window.getComputedStyle(box2).getPropertyValue('width'))</span></span><br><span class="line"><span class="regexp">&lt;/</span>script&gt;</span><br><span class="line">&lt;<span class="regexp">/body&gt;</span></span><br><span class="line"><span class="regexp">&lt;/</span>html&gt;</span><br></pre></td></tr></table></figure><p><strong>参考链接</strong></p><p><a href="https://juejin.im/post/5be03f4fe51d45053a454e69#heading-3" target="_blank" rel="noopener">如何判断元素是否进入可视区域viewport</a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;关于元素的大小&quot;&gt;&lt;a href=&quot;#关于元素的大小&quot; class=&quot;headerlink&quot; title=&quot;关于元素的大小&quot;&gt;&lt;/a&gt;关于元素的大小&lt;/h2&gt;&lt;h3 id=&quot;关于尺寸的属性&quot;&gt;&lt;a href=&quot;#关于尺寸的属性&quot; class=&quot;headerlink
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="JavaScript" scheme="https://github.com/tags/JavaScript/"/>
    
  </entry>
  
  <entry>
    <title>redux源码浅读</title>
    <link href="https://github.com/2019/02/12/redux%E6%BA%90%E7%A0%81%E6%B5%85%E8%AF%BB/"/>
    <id>https://github.com/2019/02/12/redux%E6%BA%90%E7%A0%81%E6%B5%85%E8%AF%BB/</id>
    <published>2019-02-12T07:50:28.691Z</published>
    <updated>2019-11-22T06:46:12.031Z</updated>
    
    <content type="html"><![CDATA[<h1 id="redux源码浅读"><a href="#redux源码浅读" class="headerlink" title="redux源码浅读"></a>redux源码浅读</h1><h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>Redux是 JavaScript 状态容器，我们通常会在React的项目中搭配这个库用来处理复杂的业务逻辑。</p><p>在学习使用Redux一阶段以后，觉得有必要看一下源码，一方面提高对Redux的理解，另一方面也可以通过阅读源码来提高自身的程序设计水平。</p><h2 id="Redux基本使用"><a href="#Redux基本使用" class="headerlink" title="Redux基本使用"></a>Redux基本使用</h2><p>从实际使用出发，我们才能更加容易理解Redux的源码。</p><p>下面是Redux的基本使用。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; createStore &#125; <span class="keyword">from</span> <span class="string">'redux'</span>;</span><br><span class="line"><span class="keyword">const</span> store = createStore(reducer, preloadedState, enhancer) </span><br><span class="line"><span class="comment">// 设置监听函数</span></span><br><span class="line">store.subscribe(listener);</span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line"><span class="comment">// 首先，用户派发action</span></span><br><span class="line">store.dispatch(action);</span><br><span class="line"><span class="comment">// 然后，Store自动调用Reducer，并且传入两个参数：当前State和收到的Action。Reducer会返回新的State</span></span><br><span class="line"><span class="keyword">let</span> nextState = reducer(previousState, action);</span><br><span class="line"><span class="comment">// State一旦有变化，Store就会调用监听函数，监听函数更新state，重新render</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">listerner</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> newState = store.getState();</span><br><span class="line">  component.setState(newState);   </span><br><span class="line">&#125;</span><br><span class="line">listener();</span><br></pre></td></tr></table></figure><h4 id="流程图解"><a href="#流程图解" class="headerlink" title="流程图解"></a>流程图解</h4><p>在利用Redux进行状态管理时，用户在UI层面触发行为，一个action对象通过<code>store.dispatch</code>派发到Reducer进行触发，接下来Reducer会根据type来更新对应的Store上的状态树，更改后的state会触发对应组件的重新渲染。</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g2w972tmz5j318a0o4gnp.jpg" alt=""></p><p>如果存在中间件的话，一个action对象在通过<code>store.dispatch</code>派发，在调用reducer函数前，会先经过一个中间件环节。在一个Redux架构中可以用多个中间件，这些中间件一起组织处理请求的“管道”。一个中间件是一个独立的函数，可以组合使用，中间件有一个统一的接口，正因为一个中间件只能完成一个特定的功能，所以把多个中间件组合在一起才能满足比较丰富的应用需求。当然在使用时，也需要按照顺序依次处理传入的action，只有排在前面的中间件完成任务之后，后面的中间件才能有机会继续处理action。</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g2w97n5xwdj31880fsdh3.jpg" alt=""></p><h2 id="源码简介"><a href="#源码简介" class="headerlink" title="源码简介"></a>源码简介</h2><p>先开看下 Redux 的源码的目录结构，没错，只有下面几个js，果然是短小精悍，只有2kb，而且生产环境几乎没有什么外部的依赖。虽然结构看起来并不复杂，内容也不是很长，但是Redux 作为函数式编程的典型代表，理解起来并不简单。</p><p>我们截取了 github上 的源码，如下图。</p><p><img src="/Users/apple/Library/Application%20Support/typora-user-images/image-20190510141319037.png" alt="image-20190510141319037"></p><p>所有的js源码文件如下</p><ul><li>applyMiddleware.js     // Redux的插件机制实现</li><li>bindActionCreators.js  // 将ActionCreator和dispatch绑定的工具</li><li>combineReducers.js     // 将store分层的工具</li><li>compose.js             // 将数个函数合并嵌套执行</li><li>createStore.js         // 核心，生成store</li><li>index.js               // 入口</li></ul><h2 id="源码解析"><a href="#源码解析" class="headerlink" title="源码解析"></a>源码解析</h2><p>入口文件index.js和bindActionCreators这边就不做介绍了。下面展示的源码部分都是精简版的，去掉了一些注释和无关主逻辑的代码。</p><h3 id="createStore"><a href="#createStore" class="headerlink" title="createStore"></a>createStore</h3><p>首先来看createStore ，它的作用就是创建全局的store对象。在 Redux 中，整个应用只能有一个 Store，全局的state对象就保存在这个全局的store对象中。</p><h4 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h4><ul><li>参数<ul><li>reducer<ul><li>数据类型：函数</li><li>参数含义：接受当前state对象和action作为参数，用于处理state逻辑</li></ul></li><li>preloadedState<ul><li>数据类型：：对象</li><li>参数含义：初始的state</li></ul></li><li>enhancer：<ul><li>数据类型：函数</li><li>参数含义：store的增强器。这个函数由redux提供的applyMiddleware 函数来进行生成</li></ul></li></ul></li><li>返回值<ul><li>store<ul><li>数据类型：对象</li><li>该对象最常用的有三个属性getState，subscribe，dispatch。都是函数类型的</li></ul></li></ul></li></ul><h4 id="源码"><a href="#源码" class="headerlink" title="源码"></a>源码</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> $$observable <span class="keyword">from</span> <span class="string">'symbol-observable'</span></span><br><span class="line"><span class="keyword">import</span> ActionTypes <span class="keyword">from</span> <span class="string">'./utils/actionTypes'</span></span><br><span class="line"><span class="keyword">import</span> isPlainObject <span class="keyword">from</span> <span class="string">'./utils/isPlainObject'</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">createStore</span>(<span class="params">reducer, preloadedState, enhancer</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 判断 enhancer 是否是函数，enhancer 实际上就是中间件</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> enhancer !== <span class="string">'undefined'</span>) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> enhancer !== <span class="string">'function'</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> enhancer(createStore)(reducer, preloadedState)</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 判断reducer是否是函数</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> reducer !== <span class="string">'function'</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 当前reducer</span></span><br><span class="line">  <span class="keyword">let</span> currentReducer = reducer</span><br><span class="line">  <span class="comment">// 当前状态state</span></span><br><span class="line">  <span class="keyword">let</span> currentState = preloadedState</span><br><span class="line">  <span class="comment">// 当前的监听器队列</span></span><br><span class="line">  <span class="keyword">let</span> currentListeners = []</span><br><span class="line">  <span class="comment">// 未来的监听器队列</span></span><br><span class="line">  <span class="keyword">let</span> nextListeners = currentListeners</span><br><span class="line">  <span class="comment">// 是否处于再派发action的状态，即是否在执行reducer函数。这是为了避免死循环。默认为false。</span></span><br><span class="line">  <span class="keyword">let</span> isDispatching = <span class="literal">false</span></span><br><span class="line"><span class="comment">// 这个函数用于确保currentListeners 和 nextListeners 是不同的引用</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">ensureCanMutateNextListeners</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (nextListeners === currentListeners) &#123;</span><br><span class="line">      nextListeners = currentListeners.slice()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 返回当前的state</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">getState</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (isDispatching) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> currentState</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 订阅监听函数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">subscribe</span>(<span class="params">listener</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> listener !== <span class="string">'function'</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (isDispatching) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">let</span> isSubscribed = <span class="literal">true</span></span><br><span class="line">    <span class="comment">// 这里Redux维护了两个队列，currentListeners和nextListeners</span></span><br><span class="line">    <span class="comment">// 考虑到只存在 currentListeners 的情况，如果我在执行某个 listener 中再次执行 subscribe或者 unsubscribe，会导致索引出错</span></span><br><span class="line">    <span class="comment">// 只有当下一次dispatch前，nextListeners又被同步给了currentListeners，之前的注册注销才会生效                </span></span><br><span class="line">    ensureCanMutateNextListeners()</span><br><span class="line">    nextListeners.push(listener)</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">unsubscribe</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      <span class="keyword">if</span> (!isSubscribed) &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (isDispatching) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">      &#125;</span><br><span class="line">      isSubscribed = <span class="literal">false</span></span><br><span class="line">      ensureCanMutateNextListeners()</span><br><span class="line">      <span class="keyword">const</span> index = nextListeners.indexOf(listener)</span><br><span class="line">      nextListeners.splice(index, <span class="number">1</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 派发action</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">dispatch</span>(<span class="params">action</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// action必须是一个对象</span></span><br><span class="line">    <span class="keyword">if</span> (!isPlainObject(action)) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// type必须要有属性，不能是undefined</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> action.type === <span class="string">'undefined'</span>) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 禁止在reducers中进行dispatch，因为这样做可能导致分发死循环</span></span><br><span class="line">    <span class="keyword">if</span> (isDispatching) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      isDispatching = <span class="literal">true</span></span><br><span class="line">      <span class="comment">// 将当前的状态和action传给当前的reducer，用于生成最新的state </span></span><br><span class="line">      currentState = currentReducer(currentState, action)</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">      <span class="comment">// 派发完毕</span></span><br><span class="line">      isDispatching = <span class="literal">false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> listeners = (currentListeners = nextListeners)</span><br><span class="line">    <span class="comment">// 在得到新的状态后，依次调用每个监听器函数</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; listeners.length; i++) &#123;</span><br><span class="line">      <span class="keyword">const</span> listener = listeners[i]</span><br><span class="line">      listener()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> action</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  dispatch(&#123; <span class="attr">type</span>: ActionTypes.INIT &#125;)</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    dispatch,</span><br><span class="line">    subscribe,</span><br><span class="line">    getState,</span><br><span class="line">    replaceReducer,</span><br><span class="line">    [$$observable]: observable</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h4><p>createStore就是封装了一些api，通过闭包的形式，保存了两个重要的私有变量，state和listener。</p><p><strong>getState</strong>：用来获取store中的state的。因为redux是不允许用户直接操作state，对于state的获取，是得通过getState的api来获取store内部的state。</p><p><strong>subscribe</strong>：可以给 store 的状态添加订阅监听，一旦我们调用了 dispatch 来分发 action ，所有的监听函数就会执行。实际使用中用的不多，是因为react-redux 隐式的为我们帮我们完成了这方面的工作。</p><p><strong>dispatch</strong>：是redux中一个非常核心的方法，也是我们在日常开发中最常用的方法之一。dispatch函数是用来触发状态改变的，它接受一个 action 对象作为参数，然后 reducer 就可以根据 action 的属性以及当前 store 的状态，来生成一个新的状态，从而改变 store 的状态；</p><h3 id="compose"><a href="#compose" class="headerlink" title="compose"></a>compose</h3><p>compose 可以接受一组函数作为参数，从右到左来组合多个函数，然后返回一个组合函数。其实就是函数的合并，它想要实现的效果就像下面这样。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">compose(a, b, c) === <span class="function">(<span class="params">...args</span>) =&gt;</span> a(b(c(...args)))</span><br></pre></td></tr></table></figure><h4 id="语法-1"><a href="#语法-1" class="headerlink" title="语法"></a>语法</h4><ul><li>参数<ul><li>funcs：<ul><li>数据类型：函数</li><li>参数含义：若干个用于组合的函数</li></ul></li></ul></li><li>返回值<ul><li>func：<ul><li>数据类型：函数</li><li>参数含义：执行该函数返回多个函数连续执行的结果</li></ul></li></ul></li></ul><h4 id="源码-1"><a href="#源码-1" class="headerlink" title="源码"></a>源码</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">compose</span>(<span class="params">...funcs</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (funcs.length === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="params">arg</span> =&gt;</span> arg</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (funcs.length === <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> funcs[<span class="number">0</span>]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> funcs.reduce(<span class="function">(<span class="params">a, b</span>) =&gt;</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> a(b(...args)))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>underscore 的 compose 函数的实现，可以对比一下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compose</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> args = <span class="built_in">arguments</span>;</span><br><span class="line">    <span class="keyword">var</span> start = args.length - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> i = start;</span><br><span class="line">        <span class="keyword">var</span> result = args[start].apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">        <span class="keyword">while</span> (i--) result = args[i].call(<span class="keyword">this</span>, result);</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h4 id="总结-1"><a href="#总结-1" class="headerlink" title="总结"></a>总结</h4><p>compose函数理解并不困难，就是将多个函数依次进行组合，将后者的返回结果作为前者的参数。很多js库也都有类似的函数实现，redux利用了数组reduce方法的特性巧妙得用一行代码实现了功能。</p><p>###applyMiddleware</p><p>Redux 中最难的还是applyMiddleware的理解。这个插件机制是Redux一个很重要的功能，使得我们可以很方便的对action做各种各样的操作。比如日志打印，异步数据获取等等，只要添加一个middleware，就可以实现。这个类似于Express或者Koa中的中间件机制。</p><p>你可以这样理解函数柯里化，通过闭包保存了外部的一个变量，然后返回一个接收参数的函数，在该函数中使用了保存的变量，然后再返回值。</p><p>分析applyMiddleware的代码结构。 是个三级<strong>柯里化</strong>的高阶函数。它将依次获得三个参数：第一个是 middlewares 数组，第二个是 Redux 原生的 createStore，最后一个是 reducer。</p><h4 id="语法-2"><a href="#语法-2" class="headerlink" title="语法"></a>语法</h4><ul><li>参数<ul><li>middlewares<ul><li>数据类型：函数</li><li>参数含义：若干个中间件函数</li></ul></li></ul></li><li>返回值<ul><li>func<ul><li>数据类型：函数</li><li>参数含义：高阶函数，并作为函数creatStore的参数之一，连续执行该函数可以得到最终的store对象</li></ul></li></ul></li></ul><h4 id="源码-2"><a href="#源码-2" class="headerlink" title="源码"></a>源码</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 一个最简单的中间件</span></span><br><span class="line"><span class="comment">// 所有的中间件都必须遵循这样的格式，这个是因为applyMiddleware函数决定的</span></span><br><span class="line">(&#123;getState, dispatch&#125;) =&gt; <span class="function"><span class="params">next</span> =&gt;</span> <span class="function"><span class="params">action</span> =&gt;</span> next(action);</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">applyMiddleware</span>(<span class="params">...middlewares</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="params">createStore</span> =&gt;</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 利用传入的createStore和reducer创建一个store</span></span><br><span class="line">    <span class="keyword">const</span> store = createStore(...args)</span><br><span class="line">    <span class="comment">// 这个dispatch并不是最后在中间件里</span></span><br><span class="line">    <span class="keyword">let</span> dispatch = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(...)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 传给中间件第一层函数的参数                  </span></span><br><span class="line">    <span class="keyword">const</span> middlewareAPI = &#123;</span><br><span class="line">      getState: store.getState,</span><br><span class="line">      dispatch: <span class="function">(<span class="params">...args</span>) =&gt;</span> dispatch(...args)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 让每个 middleware 执行一遍，并传入两个参数getState和dispatch</span></span><br><span class="line">    <span class="keyword">const</span> chain = middlewares.map(<span class="function"><span class="params">middleware</span> =&gt;</span> middleware(middlewareAPI))</span><br><span class="line">    <span class="comment">// 上面提到compose的作用就是组合函数，结合中间件函数的格式，举个例子</span></span><br><span class="line">    <span class="comment">// 如果存在三个中间件a,b,c。在通过上面的函数执行过后，每个中间件返回的是next =&gt; action =&gt; &#123;&#125;这样的函数。函数的参数都是next函数，再返回一个函数，并通过闭包的形式把next函数保存起来。</span></span><br><span class="line">    <span class="comment">// 执行compose(...chain)(store.dispatch)</span></span><br><span class="line">    <span class="comment">// c最先执行，参数是store.dispatch，执行返回action=&gt;&#123;dosomethingC();return dispatch(action)&#125;</span></span><br><span class="line">    <span class="comment">// b接着执行，参数是c的返回结果，然后返回action=&gt;&#123;dosomethingB();dosomethingA();return dispatch(action)&#125;</span></span><br><span class="line">    <span class="comment">// a接着执行，参数是b的返回结果，然后返回action=&gt;&#123;dosomethingA();dosomethingB();dosomethingC();return dispatch(action)&#125;，并且赋值给变量dispatch</span></span><br><span class="line">    dispatch = compose(...chain)(store.dispatch)</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">      ...store,</span><br><span class="line">      dispatch</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="中间件的执行顺序"><a href="#中间件的执行顺序" class="headerlink" title="中间件的执行顺序"></a>中间件的执行顺序</h4><p>还有特别重要的就是关于中间件的执行顺序。下面是一个测试的demo。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">middleware1</span>(<span class="params">&#123;dispatch,getState&#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">next</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'middleware1 next层'</span>,next);</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">action</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware1 action层 开始'</span>)</span><br><span class="line">      next(action)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware1 action层 结束'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">middleware2</span>(<span class="params">&#123;dispatch,getState&#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">next</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'middleware2 next层'</span>,next);</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">action</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware2 action层 开始'</span>)</span><br><span class="line">      next(action)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware2 action层 结束'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">middleware3</span>(<span class="params">&#123;dispatch,getState&#125;</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">next</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'middleware3 next层'</span>,next);</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">action</span>) </span>&#123;</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware3 action层 开始'</span>)</span><br><span class="line">      next(action)</span><br><span class="line">      <span class="built_in">console</span>.log(<span class="string">'middleware3 action层 结束'</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> noop = <span class="function"><span class="params">()</span>=&gt;</span>&#123;&#125;</span><br><span class="line"><span class="comment">// 执行第一层函数</span></span><br><span class="line"><span class="keyword">const</span> chain = [middleware1, middleware2, middleware3].map(<span class="function"><span class="params">middleware</span> =&gt;</span> middleware(&#123;noop,noop&#125;))</span><br><span class="line"><span class="comment">// 执行第二层函数</span></span><br><span class="line"><span class="keyword">let</span> dispatch = compose(...chain)(noop)</span><br><span class="line"><span class="comment">// 结果如下 </span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  middleware3 next层 ()=&gt;&#123;&#125;</span></span><br><span class="line"><span class="comment"> middleware2 next层 ƒ (action) &#123;</span></span><br><span class="line"><span class="comment">    console.log('middleware3 action层 开始')</span></span><br><span class="line"><span class="comment">    next(action)</span></span><br><span class="line"><span class="comment">    console.log('middleware3 action层 结束')</span></span><br><span class="line"><span class="comment">   &#125;</span></span><br><span class="line"><span class="comment">  middleware1 next层 ƒ (action) &#123;</span></span><br><span class="line"><span class="comment">    console.log('middleware2 action层 开始')</span></span><br><span class="line"><span class="comment">    next(action)</span></span><br><span class="line"><span class="comment">    console.log('middleware2 action层 结束')</span></span><br><span class="line"><span class="comment">  &#125;</span></span><br><span class="line"><span class="comment">**/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 执行第三层函数</span></span><br><span class="line">dispatch()</span><br><span class="line"><span class="comment">// 结果如下 </span></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">  middleware1 action层 开始</span></span><br><span class="line"><span class="comment">  middleware2 action层 开始</span></span><br><span class="line"><span class="comment">  middleware3 action层 开始</span></span><br><span class="line"><span class="comment">  middleware3 action层 结束</span></span><br><span class="line"><span class="comment">  middleware2 action层 结束</span></span><br><span class="line"><span class="comment">  middleware1 action层 结束</span></span><br><span class="line"><span class="comment">**/</span></span><br></pre></td></tr></table></figure><h4 id="总结-2"><a href="#总结-2" class="headerlink" title="总结"></a>总结</h4><p>中间件链的最内环是接受<code>store.dispatch</code>作为参数，最后返回的dispatch是增强的dispatch。每个中间件的next也是一次封装增强后的dispath。</p><h3 id="combineReducers"><a href="#combineReducers" class="headerlink" title="combineReducers"></a>combineReducers</h3><p>这个函数是用来整合多个reducers的， 因为createStore只接受一个reducer作为参数。</p><h4 id="语法-3"><a href="#语法-3" class="headerlink" title="语法"></a>语法</h4><ul><li>参数<ul><li>reducer<ul><li>数据类型：对象</li><li>参数含义：属性值是所有的reducer函数</li></ul></li></ul></li><li>返回值<ul><li>finalreducer：<ul><li>数据类型：函数</li><li>参数含义：reducer合并返回最终的reducer函数</li></ul></li></ul></li></ul><h4 id="源码-3"><a href="#源码-3" class="headerlink" title="源码"></a>源码</h4><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">combineReducers</span>(<span class="params">reducers</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 获取该对象的 key 值</span></span><br><span class="line">  <span class="keyword">const</span> reducerKeys = <span class="built_in">Object</span>.keys(reducers)</span><br><span class="line">  <span class="comment">// 有效的reducer列表</span></span><br><span class="line">  <span class="keyword">const</span> finalReducers = &#123;&#125;</span><br><span class="line">  <span class="comment">// 过滤无效的reducer</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; reducerKeys.length; i++) &#123;</span><br><span class="line">    <span class="keyword">const</span> key = reducerKeys[i]</span><br><span class="line">    <span class="keyword">if</span> (process.env.NODE_ENV !== <span class="string">'production'</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">typeof</span> reducers[key] === <span class="string">'undefined'</span>) &#123;</span><br><span class="line">        warning(<span class="string">`No reducer provided for key "<span class="subst">$&#123;key&#125;</span>"`</span>)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> reducers[key] === <span class="string">'function'</span>) &#123;</span><br><span class="line">      finalReducers[key] = reducers[key]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 拿到过滤后的reducers的key值</span></span><br><span class="line">  <span class="keyword">const</span> finalReducerKeys = <span class="built_in">Object</span>.keys(finalReducers)</span><br><span class="line"><span class="comment">// 返回最终生成的reducer</span></span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span> <span class="title">combination</span>(<span class="params">state = &#123;&#125;, action</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 定义state是否改变，默认false</span></span><br><span class="line">    <span class="keyword">let</span> hasChanged = <span class="literal">false</span></span><br><span class="line">    <span class="comment">// 定义新的nextState</span></span><br><span class="line">    <span class="keyword">const</span> nextState = &#123;&#125;</span><br><span class="line">    <span class="comment">// 遍历reducers对象中的有效key，执行该key对应的value函数，即子reducer函数，并得到对应的state对象,将新的子state挂到新的nextState对象上</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; finalReducerKeys.length; i++) &#123;</span><br><span class="line">      <span class="keyword">const</span> key = finalReducerKeys[i]</span><br><span class="line">      <span class="keyword">const</span> reducer = finalReducers[key]</span><br><span class="line">      <span class="comment">// state树下的key是与 finalReducers下的key相同的</span></span><br><span class="line">      <span class="keyword">const</span> previousStateForKey = state[key]</span><br><span class="line">      <span class="keyword">const</span> nextStateForKey = reducer(previousStateForKey, action)</span><br><span class="line">      nextState[key] = nextStateForKey</span><br><span class="line">      <span class="comment">// 如果存在触发某个reducer函数返回的state发生了改变，hasChanged置为true</span></span><br><span class="line">      hasChanged = hasChanged || nextStateForKey !== previousStateForKey</span><br><span class="line">    &#125;</span><br><span class="line">     <span class="comment">// 遍历一遍看是否发生改变，发生改变了返回新的state，否则返回原先的state</span></span><br><span class="line">    <span class="keyword">return</span> hasChanged ? nextState : state</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="总结-3"><a href="#总结-3" class="headerlink" title="总结"></a>总结</h4><p>这个combineReducers函数分为两部分，第一部分是检验传入的reducers的准确性，得到一个过滤后的对象 finalReducers。第二部分就是计算state，根据传入的action，遍历finalReducers，执行每个对象中的每个reducer函数，并将最终的state返回。注意的是，在reducer的代码处理逻辑中，如果没有返回一个新的state独享，而是在原有的state对象上增加或删除某个属性并返回。在比较新旧state的时候，会认为两个对象是一样的，最终不会</p><h2 id="总结-4"><a href="#总结-4" class="headerlink" title="总结"></a>总结</h2><p>Redux设计巧妙，思维严谨，整个代码没有特别臃肿的部分。</p><p>虽然个人还存在不理解的部分，比如creatstore的设计思想等等。</p><p>下面还要继续学习，下一步的阅读计划是react-redux和redux-thunk，希望能趁热打铁把react全家桶的相关代码都阅读学习一遍。</p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h1 id=&quot;redux源码浅读&quot;&gt;&lt;a href=&quot;#redux源码浅读&quot; class=&quot;headerlink&quot; title=&quot;redux源码浅读&quot;&gt;&lt;/a&gt;redux源码浅读&lt;/h1&gt;&lt;h2 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; 
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="Redux" scheme="https://github.com/tags/Redux/"/>
    
  </entry>
  
  <entry>
    <title>函数式编程</title>
    <link href="https://github.com/2019/02/12/%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B/"/>
    <id>https://github.com/2019/02/12/%E5%87%BD%E6%95%B0%E5%BC%8F%E7%BC%96%E7%A8%8B/</id>
    <published>2019-02-12T07:50:28.691Z</published>
    <updated>2019-11-22T07:06:10.986Z</updated>
    
    <content type="html"><![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>在介绍函数式编程之前，我们先来看下函数在JS中的作用。在JS中，函数是一等公民，除了传统函数的使用，它可以作为普通变量一样作为函数参数，还可以在函数中被返回。除此之外，函数还可以当做类来使用，我们使用new加上函数就可以构造一个实例，并且子类还可以通过构造函数的prototype属性实现原型继承。在React中，组件其实也是一个函数。所以在JS中，函数无处不在。</p><p>在软件开发中，我们常见的编程形式有命令式编程和声明式编程。而函数式编程是一种较为抽象的编程方式，是一种强调以函数为主要开发风格的编程方式。在函数式编程中，我们以函数的形式思考和编程。</p><p>下面，我们简单来看下命令式编程和声明式编程的区别。</p><ul><li>例子1</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 命令式方式</span></span><br><span class="line"><span class="keyword">var</span> arr = [<span class="number">0</span>, <span class="number">1</span>, <span class="number">2</span>]</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; array.length; i++) &#123;</span><br><span class="line">  arr[i] = arr[i] * <span class="number">2</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">arr; <span class="comment">// [0, 2, 4]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明式方式</span></span><br><span class="line">arr.map(<span class="function"><span class="params">num</span> =&gt;</span> num *<span class="number">2</span>)</span><br></pre></td></tr></table></figure><ul><li>例子2</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 命令式方式</span></span><br><span class="line"><span class="built_in">document</span>.getElementById(<span class="string">'#root'</span>).innerHTML = <span class="string">'&lt;div&gt;根节点&lt;/div&gt;'</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明式方式</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">insertMsg</span>(<span class="params">domId, msg, format</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!domId) <span class="keyword">return</span>;</span><br><span class="line">  <span class="built_in">document</span>.querySelector(domId).innerHTML = <span class="string">`&lt;<span class="subst">$&#123;format&#125;</span>&gt;<span class="subst">$&#123;msg&#125;</span>&lt;/<span class="subst">$&#123;format&#125;</span>&gt;`</span></span><br><span class="line">&#125;</span><br><span class="line">insertMsg(<span class="string">'#root'</span>,<span class="string">'根节点'</span>,<span class="string">'div'</span>)</span><br></pre></td></tr></table></figure><p>可以看到，命令式的方式很具体的告诉计算机如何执行某个任务，先做什么再做什么。而声明式的方式主要思想是告诉计算机应该做什么，但不指定具体要怎么做。函数式编程和声明式编程很像，因为他们思想是一致的：即只关注做什么而不是怎么做。但函数式编程不仅仅局限于声明式编程。</p><p><strong>总结：</strong>这种命令式的写法比较简单，也最容易想到，开发起来很快。缺点显而易见，程序的复用性很差。如果我们有很多类似的这种操作，这就有问题了。函数式编程优点如下。</p><ul><li><p>扩展性：可以通过增加一些额外的代码，而不用修改之前的逻辑来实现功能的扩展</p></li><li><p>模块化：代码高度解耦，互不影响</p></li><li><p>重用性：可以相互使用，实现重用</p></li><li><p>抽象性：隐藏了很多实现的细节</p></li><li><p>易测试：很容易看出是哪部分代码出了问题</p></li></ul><h2 id="分类"><a href="#分类" class="headerlink" title="分类"></a>分类</h2><h3 id="高阶函数"><a href="#高阶函数" class="headerlink" title="高阶函数"></a>高阶函数</h3><p>高阶函数是对其他函数进行操作的函数。满足下列条件之一的函数即可称之为</p><ul><li>接受一个或多个函数作为参数</li><li>输出一个函数</li></ul><p>举例，最常见的是数组内置的一些方法，map和forEach，参数就是一个函数。</p><p>高阶函数的优势很明显。下面的例子一目了然。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 不使用用高阶函数</span></span><br><span class="line"><span class="keyword">const</span> arr1 = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"><span class="keyword">let</span> arr2 = [];</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>; i&lt;arr1.length; i++) &#123;</span><br><span class="line">  <span class="keyword">if</span> (arr1[i]&gt;<span class="number">3</span>) &#123;</span><br><span class="line">    arr2.push(arr1[i]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.log(arr2);  <span class="comment">// [4，5]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用filter</span></span><br><span class="line"><span class="keyword">let</span> arr3 = arr1.filter(<span class="function">(<span class="params">ele, index, self</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> ele &gt; <span class="number">3</span>;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">console</span>.log(arr3);</span><br></pre></td></tr></table></figure><h4 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h4><p><strong>一、柯里化(curring)</strong></p><p>柯里化又称部分求值。一个柯里化的函数首先会接受一些参数，接受了这些参数之后，该函数并不会立即求值，而是继续返回另外一个函数，刚才传入的参数在函数形成的闭包中被保存起来。待到函数中被真正的需要求值的时候，之前传入的所有参数被一次性用于求值。</p><p>柯里化是函数式编程的一个重要技巧，将使用多个参数的一个函数转换成一系列使用一个参数的函数的技术。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 用闭包把传入参数保存起来，当传入参数的数量足够执行函数时，就开始执行函数</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">curry</span>(<span class="params">fn</span>) </span>&#123;</span><br><span class="line">  <span class="comment">// 如果传入的函数只有一个参数，直接返回该函数</span></span><br><span class="line">  <span class="keyword">if</span> (fn.length &lt;= <span class="number">1</span>) <span class="keyword">return</span> fn;</span><br><span class="line">  <span class="keyword">const</span> generator = <span class="function">(<span class="params">...args</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// 判断当前参数长度够不够，参数够了就立马执行</span></span><br><span class="line">    <span class="keyword">if</span> (fn.length === args.length) &#123;</span><br><span class="line">      <span class="keyword">return</span> fn(...args)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="function">(<span class="params">...args2</span>) =&gt;</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> generator(...args, ...args2)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> generator</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> add = <span class="function">(<span class="params">a, b, c, d</span>) =&gt;</span> a + b + c + d</span><br><span class="line"><span class="keyword">const</span> curryiedAdd = curry(add);</span><br><span class="line">curryiedAdd(<span class="number">1</span>)(<span class="number">2</span>)(<span class="number">3</span>)(<span class="number">4</span>)</span><br></pre></td></tr></table></figure><p>很多同学知道柯里化的原理，但是不了解它具体在实际编程中的运用。</p><p>下面我举几个例子。</p><ul><li>例子1，关于网络请求</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 常见的写法</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fetch</span>(<span class="params">type,url,data</span>)</span>&#123;</span><br><span class="line"><span class="keyword">let</span> xhr = <span class="keyword">new</span> XMLHttpRequest();</span><br><span class="line">  xhr.open(type, url);</span><br><span class="line">  xhr.send(data);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 虽然很通用，但是还是有一些重复代码，而且get和post也不是一眼能区分</span></span><br><span class="line">fetch(<span class="string">'get'</span>, <span class="string">'www.test.com'</span>, <span class="string">''</span>);</span><br><span class="line">fetch(<span class="string">'post'</span>, <span class="string">'www.test.com'</span>, <span class="string">'num=10'</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用curry</span></span><br><span class="line"><span class="keyword">let</span> fetch = curry(fetch)</span><br><span class="line"><span class="comment">// 所有post类型的请求都可以使用这个方法</span></span><br><span class="line"><span class="keyword">let</span> post = fetch(<span class="string">'post'</span>);</span><br><span class="line"><span class="comment">// 所有xx接口的post类型的请求都可以使用这个方法</span></span><br><span class="line"><span class="keyword">let</span> postTest = post(<span class="string">'www.test.com'</span>);</span><br><span class="line">postTest(<span class="string">'num=10'</span>)</span><br></pre></td></tr></table></figure><ul><li>例子2，关于JS类型判断</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用对象原型方法toString可以判断几乎所有数据类型</span></span><br><span class="line"><span class="keyword">let</span> isString = <span class="function"><span class="params">obj</span> =&gt;</span> <span class="built_in">Object</span>.prototype.toString.call( obj ) === <span class="string">'[object String]'</span>;</span><br><span class="line"><span class="keyword">let</span> isRegExp = <span class="function"><span class="params">obj</span> =&gt;</span> <span class="built_in">Object</span>.prototype.toString.call( obj ) === <span class="string">'[object RegExp]'</span>;</span><br><span class="line"><span class="comment">// 还是一样的的问题，会有很多重复的代码部分，可以抽象出一些重复的部分</span></span><br><span class="line"><span class="keyword">const</span> isType = <span class="function"><span class="params">type</span> =&gt;</span> <span class="function"><span class="params">target</span> =&gt;</span> <span class="string">`[object <span class="subst">$&#123;type&#125;</span>]`</span> === <span class="built_in">Object</span>.prototype.toString.call(target);</span><br><span class="line"><span class="keyword">const</span> isArray = isType(<span class="string">'Array'</span>);</span><br><span class="line">isArray([])</span><br></pre></td></tr></table></figure><ul><li>例子3，bind的实现</li></ul><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// bind简化实现，原理就是利用柯里化实现延迟计算</span></span><br><span class="line"><span class="comment">// 使用es6语法实现起来非常简洁</span></span><br><span class="line"><span class="built_in">Function</span>.prototype.bind = <span class="function"><span class="keyword">function</span> (<span class="params">context, ...args</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> fn = <span class="keyword">this</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">...otherArgs</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> fn.apply(context, [...args, ...otherArgs] );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">  name: <span class="string">'abc'</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> myfun = <span class="function"><span class="keyword">function</span>(<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> bindmyfun = myfun.bind(obj);</span><br><span class="line">bindmyfun();</span><br></pre></td></tr></table></figure><p><strong>总结：</strong>通过上面的例子，我们得出柯里化的主要作用就是延迟计算，参数复用。</p><p><strong>二、惰性加载</strong></p><p>惰性加载表示函数执行的分支仅会发生一次。举例，如果做PC开发的时候，我们通常需要对不同的浏览器进行事件监听和移除的方法的一个兼容。</p><p>下面是正常的写法。</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> addEvent = <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">window</span>.addEventListener) &#123;</span><br><span class="line">    <span class="keyword">return</span> ele.addEventListener(type, fn, <span class="literal">false</span>);</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">window</span>.attachEvent) &#123;</span><br><span class="line">    <span class="keyword">return</span> ele.attachEvent(<span class="string">'on'</span> + type, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">      fn.call(ele);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>但是，每次在使用这个方法的时候都会执行if else这个判断，其实对于同一个浏览器而言，只需要判断一次环境就可以了。</p><ul><li>解决方案1</li></ul><p>我们在函数被调用的时候重载函数。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> addEvent = <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">window</span>.addEventListener) &#123;</span><br><span class="line">    addEvent = <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">      ele.addEventListener(type, fn, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span>  <span class="keyword">if</span> (<span class="built_in">window</span>.attachEvent) &#123;</span><br><span class="line">    addEvent = <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">      ele.attachEvent(<span class="string">'on'</span> + type, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        fn.call(ele)</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  addEvent(ele, type, fn);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ul><li>解决方案2</li></ul><p>我们在声明函数时就指定适当函数。这样，第一次调用函数就不会损失性能了，而在代码首次加载时会损失一点性能</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> addEvent = (<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">window</span>.addEventListener) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">      ele.addEventListener(type, fn, <span class="literal">false</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">else</span>  <span class="keyword">if</span> (<span class="built_in">window</span>.attachEvent) &#123;</span><br><span class="line">    addEvent = <span class="function"><span class="keyword">function</span>(<span class="params">ele, type, fn</span>) </span>&#123;</span><br><span class="line">      ele.attachEvent(<span class="string">'on'</span> + type, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">        fn.call(ele)</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure><p>以上两种方式都只在第一次执行函数时做了性能检测，省去了后续调用时多余的操作，优化了性能。</p><p><strong>三、函数节流和防抖</strong></p><p>我们都知道在scroll，suggest这样的场景下，函数会被频繁的触发，这样很消耗性能，会造成浏览器的卡顿。</p><p>比较好的解决方案就是控制函数被触发的频率，也就是函数节流了。</p><p>节流的定义：如果你持续触发事件，每隔一段时间，只执行一次事件。</p><p>节流的原理很简单：利用setTimeout和闭包。</p><p>关于节流的实现，有两种主流的实现方式，一种是使用时间戳，一种是设置定时器。</p><p>下面介绍的这种方法利用的是定时器的这种形式</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 超级简易版</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">throttle</span>(<span class="params">func, wait</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> timeout;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> context = <span class="keyword">this</span>;</span><br><span class="line">    <span class="keyword">if</span> (!timeout) &#123;</span><br><span class="line">      timeout = setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        timeout = <span class="literal">null</span>;</span><br><span class="line">        func.apply(context, args)</span><br><span class="line">      &#125;, wait)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="纯函数"><a href="#纯函数" class="headerlink" title="纯函数"></a>纯函数</h2><p>纯函数的特征如下。</p><ul><li>只依赖于它的参数，同时对于任何相同的输入有着相同的输出结果。</li></ul><ul><li>不对外产生副作用</li></ul><p>如何理解？</p><p>首先来解释第一点：<strong>函数的返回结果只依赖于它的参数</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 非纯函数</span></span><br><span class="line"><span class="comment">// 返回结果依赖外部变量a，a的值是不确定的，不能保证相同的输入有相同的结果</span></span><br><span class="line"><span class="keyword">const</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">const</span> fun1 = <span class="function">(<span class="params">b</span>) =&gt;</span> a + b</span><br><span class="line"><span class="comment">// Math.random()产生一个随机数，所以返回结果也不确定</span></span><br><span class="line"><span class="keyword">const</span> fun2 = <span class="function">(<span class="params">x</span>) =&gt;</span> <span class="built_in">Math</span>.random() + x</span><br><span class="line"></span><br><span class="line"><span class="comment">// 纯函数</span></span><br><span class="line"><span class="keyword">const</span> fun3 = <span class="function">(<span class="params">x</span>) =&gt;</span> x * <span class="number">2</span></span><br></pre></td></tr></table></figure><p>接下去说一下第二点：副作用是指：<strong>在计算结果的过程中，系统状态的一种变化，或者与外部世界进行的可观察的交互</strong>。比如说调用 DOM API 修改页面，或者你发送了 Ajax 请求，改变参数或者外部变量的值，还有调用 <code>window.reload</code>刷新浏览器都是副作用的一种表现。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 非纯函数</span></span><br><span class="line"><span class="comment">// 改变了外部变量a</span></span><br><span class="line"><span class="keyword">let</span> a = &#123; <span class="attr">num</span>: <span class="number">11</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">set</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line"> x.num++;</span><br><span class="line"> <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">set</span>(a)</span><br><span class="line"></span><br><span class="line">// 纯函数</span><br><span class="line">let a = &#123; num: <span class="number">11</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">set</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line"> <span class="keyword">let</span> b = &#123;...x&#125;;</span><br><span class="line"> b.num ++;</span><br><span class="line"> <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">set</span>(x)</span><br></pre></td></tr></table></figure><p>我们看到纯函数的输出结果是一致的，可预测的，相同的输入会有相同的返回值。</p><p>为什么使用纯函数？看下面的例子</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> num = <span class="number">0</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">increment</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> ++num;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个函数读取了外部的变量，可能会觉得这段代码没有什么问题，但是我们要知道这种依赖外部变量来进行的计算，计算结果很难预测，你也有可能在其他地方修改了 num 的值，导致你 increment 出来的值不是你预期的，而且你多次调用这个函数返回的结果是不一样的，这样造成的结果就是很难定位到问题发生的原因和位置。</p><h4 id="应用-1"><a href="#应用-1" class="headerlink" title="应用"></a>应用</h4><p>最常见的应用就是<code>redux</code>中的reducer。reducer必须是一个纯函数，它会接受一个当前的state和action作为参数，返回一个全新的state，但是不能在原有的state基础上修改。</p><p>所以我们常常会见到这样的处理方式</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">return</span> <span class="built_in">Object</span>.assign(&#123;&#125;, state, ...x);</span><br><span class="line"><span class="keyword">return</span> &#123;...state,...x&#125;</span><br></pre></td></tr></table></figure><p>那如果我们直接在state上修改，会造成什么结果呢？结果就是页面不会触发重渲染。</p><p>所以这就是为什么要求reducer必须是一个纯函数呢，不能修改外部变量。</p><p>下面是redux中<strong>combineReducers</strong>函数的部分源码截图</p><p><img src="https://ws3.sinaimg.cn/large/006tNc79ly1g3qk04gcxdj30h50920u3.jpg" alt=""></p><p>可以看到在代码中，我们通过对比旧的state对象和新的state对象的地址指针，来决定状态标志位hasChanged是true还是false，从而决定返回的新或旧的state。所以，如果我们直接改变旧state的属性，那preState肯定是等于nextState。</p><p>Redux通过对比新旧对象的存储位置是不是一样，来决定state是不是发生了变化。</p><p>为什么这样设计呢？答案很简单，性能好。试想一下，如果state层级很深，而我们不是简单的通过<code>preState===nextState</code>来判断state是不是发生变化，那我们唯一的方式就是深对比，通过不断遍历对象，然后通过递归，复杂度可想而知。</p><p><strong>总结</strong></p><ul><li><p>可复用性：纯函数仅依赖于传入的参数，这意味着你可以随意将这个函数移植到别的代码中</p></li><li><p>可测试性：纯函数非常容易进行单元测试，因为不需要考虑上下文环境，只需要考虑输入和输出。</p></li><li><p>并行代码：纯函数是健壮的，改变执行次序不会对系统造成影响，因此纯函数的操作可以并行执行。</p></li></ul><p>最终目的是：<strong>让你的代码尽可能简单易懂和灵活</strong>。</p><h2 id="函数组合"><a href="#函数组合" class="headerlink" title="函数组合"></a>函数组合</h2><p>函数组合是一个数学概念，允许你将两个或更多个函数组合到一个新函数中，就像搭积木一样，最后用一个函数实现所有函数的功能。</p><p>例如，我们有这样一个需求。输入一个字符串，过滤掉两遍的空格，全部转为大写，重复三遍，最后格式化一下，插入到指定dom中。</p><p>于是，我们就开始写代码了</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> str = <span class="string">'angle'</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleStr</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">  x = x.replace(<span class="regexp">/^(\s*)|(\s*)$/g</span>,<span class="string">''</span>);</span><br><span class="line">  x = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">4</span>).join(x);</span><br><span class="line">  <span class="keyword">let</span> html = <span class="string">`&lt;h1&gt;i am <span class="subst">$&#123;x&#125;</span>&lt;/h1&gt;`</span></span><br><span class="line">  <span class="built_in">document</span>.getElementById(<span class="string">'#root'</span>).innerHTML = html</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p> 如果有一天，我们还有另外一个业务。不需要过滤空格，格式化为div，重复五遍，最后插入到anotherDom中。于是又写了下面这个函数。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">handleStr</span>(<span class="params">x</span>) </span>&#123;</span><br><span class="line">  x = <span class="keyword">new</span> <span class="built_in">Array</span>(<span class="number">6</span>).join(x);</span><br><span class="line">  <span class="keyword">let</span> html = <span class="string">`&lt;div&gt;i am <span class="subst">$&#123;x&#125;</span>&lt;/div&gt;`</span></span><br><span class="line">  <span class="built_in">document</span>.getElementById(<span class="string">'#anotherDom'</span>).innerHTML = html</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>缺点显而易见了吧。代码冗余，不好理解，很难复用。</p><p>所以，如果我们这样写，是不是会优雅很多。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> trim = <span class="function">(<span class="params">x</span>) =&gt;</span> x.replace(<span class="regexp">/^(\s*)|(\s*)$/g</span>,<span class="string">''</span>);</span><br><span class="line"><span class="keyword">const</span> repeat = <span class="function">(<span class="params">n, x</span>) =&gt;</span> <span class="keyword">new</span> <span class="built_in">Array</span>(n + <span class="number">1</span>).join(x);</span><br><span class="line"><span class="keyword">const</span> format = <span class="function">(<span class="params">format, x</span>) =&gt;</span> <span class="string">`&lt;<span class="subst">$&#123;format&#125;</span>&gt;i am <span class="subst">$&#123;x&#125;</span>&lt;/<span class="subst">$&#123;format&#125;</span>&gt;`</span></span><br><span class="line"><span class="keyword">const</span> insertMsg = <span class="function">(<span class="params">domId, x</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">if</span> (!domId) <span class="keyword">return</span>;</span><br><span class="line">  <span class="built_in">document</span>.querySelector(domId).innerHTML = x</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这边需要利用curry函数将多个参数的函数进行柯里化</span></span><br><span class="line"><span class="keyword">const</span> handleStr = compose(curry(insertMsg)(<span class="string">'#root'</span>), curry(format)(<span class="string">'h1'</span>), curry(repeat)(<span class="number">3</span>), trim);</span><br></pre></td></tr></table></figure><p>那compose函数到底是如何实现的呢</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 我的实现方式，比较low，但是比较容易看懂</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compose</span>(<span class="params">...funcs</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> res = funcs[funcs.length<span class="number">-1</span>](...args)</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = (funcs.length<span class="number">-2</span>); i&gt;=<span class="number">0</span>; i--) &#123;</span><br><span class="line">      res = funcs[i](res)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// redux的实现</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compose</span>(<span class="params">...funcs</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (funcs.length === <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="function"><span class="params">arg</span> =&gt;</span> arg</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (funcs.length === <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> funcs[<span class="number">0</span>]</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> funcs.reduce(<span class="function">(<span class="params">a, b</span>) =&gt;</span> <span class="function">(<span class="params">...args</span>) =&gt;</span> a(b(...args)))</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// underscore的实现</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">compose</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">var</span> args = <span class="built_in">arguments</span>;</span><br><span class="line">  <span class="keyword">var</span> start = args.length - <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">var</span> i = start;</span><br><span class="line">    <span class="keyword">var</span> result = args[start].apply(<span class="keyword">this</span>, <span class="built_in">arguments</span>);</span><br><span class="line">    <span class="keyword">while</span> (i--) result = args[i].call(<span class="keyword">this</span>, result);</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><h2 id="函数记忆"><a href="#函数记忆" class="headerlink" title="函数记忆"></a>函数记忆</h2><p>函数记忆是指将上次的计算结果缓存起来，再次调用的时候如果遇到相同的参数，就直接返回缓存中的结果。</p><p>比如下面这样</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> add = <span class="function">(<span class="params">x, y</span>) =&gt;</span> x + y;</span><br><span class="line"><span class="comment">// 我们实现这样一个函数</span></span><br><span class="line">add = memory(add);</span><br><span class="line">add(<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line"><span class="comment">// 相同的参数，第二次调用的时候直接从缓存中取出数据，而非重新计算</span></span><br><span class="line">add(<span class="number">1</span>,<span class="number">2</span>)</span><br></pre></td></tr></table></figure><p>下面我们来实现以下</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> memory = <span class="function"><span class="keyword">function</span> (<span class="params">fn</span>) </span>&#123;</span><br><span class="line"><span class="keyword">let</span> cache = &#123;&#125;;</span><br><span class="line"><span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">...args</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// 根据参数生成一个唯一的key，这个key的生成方式还有待研究，不够严谨</span></span><br><span class="line">    <span class="keyword">let</span> key = args.join(<span class="string">'&amp;'</span>);</span><br><span class="line">    <span class="comment">// 如果key命中缓存，说明已经计算过该函数，则直接返回缓存结果</span></span><br><span class="line">    <span class="keyword">if</span> (cache.hasOwnProperty(key)) &#123;</span><br><span class="line">      <span class="keyword">return</span> cache[key]</span><br><span class="line">    &#125;</span><br><span class="line">    cache[key] = fn.apply(<span class="keyword">this</span>, args);</span><br><span class="line"> <span class="keyword">return</span> cache[key];</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>下面测试一下使用效果</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> multiply = <span class="function">(<span class="params">x, y, z</span>) =&gt;</span> x*y*z;</span><br><span class="line"><span class="keyword">let</span> memoryMultiply = memory(multiply);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.time(<span class="string">'memory'</span>)</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>; i&lt;<span class="number">10000</span>; i++) &#123;</span><br><span class="line">  memoryMultiply(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.timeEnd(<span class="string">'memory'</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.time(<span class="string">'not memory'</span>)</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i=<span class="number">0</span>; i&lt;<span class="number">10000</span>; i++) &#123;</span><br><span class="line">  multiply(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">console</span>.timeEnd(<span class="string">'not memory'</span>)</span><br></pre></td></tr></table></figure><p>在chrome控制台运行上面的程序，结果如下，出乎意料的是使用函数记忆居然比不使用要慢了很多。我自己只能理解为，这种简单计算的时间其实比去缓存中存取来得更快。那到底什么场景使用这个呢。我认为是复杂计算或者递归的场景。</p><p><img src="https://ws4.sinaimg.cn/large/006tNc79ly1g3rjue5wrzj30e6078mxn.jpg" alt=""></p><p>我们来看下著名的斐波那契数列。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> count = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">let</span> fibonacci = <span class="function"><span class="keyword">function</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  count++;</span><br><span class="line">    <span class="keyword">return</span> n &lt; <span class="number">2</span>? n : fibonacci(n<span class="number">-1</span>) + fibonacci(n<span class="number">-2</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用普通递归</span></span><br><span class="line"><span class="built_in">console</span>.time(<span class="string">'not memory'</span>)</span><br><span class="line">fibonacci(<span class="number">20</span>);</span><br><span class="line"><span class="built_in">console</span>.timeEnd(<span class="string">'not memory'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'计算次数'</span>, count)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用函数记忆</span></span><br><span class="line"><span class="comment">// 注意这边要覆盖自身</span></span><br><span class="line">count = <span class="number">0</span>;</span><br><span class="line">fibonacci = memory(fibonacci);</span><br><span class="line"><span class="built_in">console</span>.time(<span class="string">'memory'</span>)</span><br><span class="line">fibonacci(<span class="number">20</span>);</span><br><span class="line"><span class="built_in">console</span>.timeEnd(<span class="string">'memory'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'计算次数'</span>, count)</span><br></pre></td></tr></table></figure><p><img src="https://ws1.sinaimg.cn/large/006tNc79ly1g3rk9ysp2aj30iu09e3zh.jpg" alt=""></p><p>可以看到函数真正执行计算的次数和时间大大减少，这就是使用函数记忆的优势了。</p><p><strong>总结：</strong>函数记忆只是一种编程技巧，memory函数本质上它也属于一种高阶函数。从上面的例子看得出来，并不是所有场景都适用于这种方式。而且这种方式还有一个缺点是牺牲算法的空间复杂度。所以，最适合业务场景的才是最好的。</p><h2 id="函数式编程的使用"><a href="#函数式编程的使用" class="headerlink" title="函数式编程的使用"></a>函数式编程的使用</h2><p>函数式编程的最典型应用就是Redux。</p><p>下面我们一起来看下源码的解析。</p><p><a href="https://github.com/only-twj520Q/Blog/blob/master/redux源码浅读.md"><strong>redux源码浅读</strong></a></p>]]></content>
    
    <summary type="html">
    
      
      
        &lt;h2 id=&quot;引言&quot;&gt;&lt;a href=&quot;#引言&quot; class=&quot;headerlink&quot; title=&quot;引言&quot;&gt;&lt;/a&gt;引言&lt;/h2&gt;&lt;p&gt;在介绍函数式编程之前，我们先来看下函数在JS中的作用。在JS中，函数是一等公民，除了传统函数的使用，它可以作为普通变量一样作为函数参数，还可
      
    
    </summary>
    
    
      <category term="前端" scheme="https://github.com/categories/%E5%89%8D%E7%AB%AF/"/>
    
    
      <category term="设计模式" scheme="https://github.com/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
</feed>
